// 此文件由 build.js 自动生成，请勿手动编辑
const PROBLEMS_DATA = [
    {
        "id": "prob-array-replacement",
        "title": "D. Array Replacement（差分数组上交换相邻同奇偶项）",
        "oj": "",
        "problemId": "",
        "difficulty": "困难",
        "category": "基础题",
        "tags": [
            "差分数组",
            "排序",
            "贪心",
            "字典序最小",
            "奇偶性",
            "构造"
        ],
        "timeComplexity": "O(n log n)",
        "spaceComplexity": "O(n)",
        "codePath": "题目专辑\\基础题\\ArrayReplacement\\代码.cpp",
        "content": "## 题目描述\n\n给定一个长度为 $n$ 的数组 $a$。你可以执行任意次操作（可以 0 次）：\n\n选择下标 $i$（满足 $2 \\le i \\le n-1$），要求 $a_{i-1}$ 和 $a_{i+1}$ **奇偶性相同**。把 $a_i$ 修改为：\n\n$$a_i \\leftarrow a_{i-1} - a_i + a_{i+1}$$\n\n求经过任意次操作后，能得到的**字典序最小**的数组。\n\n### 输入格式\n\n多组测试用例。\n\n- 第一行：$t$（$1 \\le t \\le 10^4$）测试用例数量\n- 每组：\n  - 第一行：整数 $n$（$3 \\le n \\le 2 \\times 10^5$）\n  - 第二行：$n$ 个整数 $a_1, a_2, \\ldots, a_n$（$-10^9 \\le a_i \\le 10^9$）\n\n保证所有测试用例的 $n$ 总和不超过 $2 \\times 10^5$。\n\n### 输出格式\n\n每组输出最终字典序最小的数组。\n\n## 解题思路\n\n### 第一步：三个基础观察\n\n**观察 1：两个端点永远不动。** 操作只改写中间位置，$a_1$ 与 $a_n$ 从头到尾不变。$a_1$ 恰好是字典序比较的第一个分量，它已固定，所以目标等价于让 $a_2$ 到 $a_n$ 这一段的字典序尽量小。\n\n**观察 2：每个位置的奇偶性不变。** 合法条件保证 $a_{i-1} + a_{i+1}$ 是偶数，新值 $a_{i-1} - a_i + a_{i+1}$ 与 $-a_i$ 同余，故与旧值同奇偶。但奇偶只是必要条件，不能直接刻画所有可达状态。\n\n**观察 3：操作是自逆的。** 对同一位置连做两次恢复原状。因此只需问\"哪些状态彼此连通\"，不必关心操作顺序。\n\n### 第二步：核心转化——换到差分数组上看\n\n**为什么想到差分**：操作公式同时牵扯相邻三项，而合法条件比较的却是相距为 2 的两项。\"改动一个值、条件看跨位邻居\"是差分的典型场景。\n\n设差分 $d_j = a_{j+1} - a_j$，共 $n-1$ 项。给定 $a_1$，原数组与差分一一对应：$a_k = a_1 + \\sum_{j=1}^{k-1} d_j$。\n\n**操作在差分上的效果**：一次操作只改 $a_i$。但 $a_i$ 同时是 $d_{i-1}$ 的右端点和 $d_i$ 的左端点，所以 $a_i$ 一变，两个差分都必须重算：\n\n$$d'_{i-1} = a'_i - a_{i-1} = a_{i+1} - a_i = d_i$$\n$$d'_i = a_{i+1} - a'_i = a_i - a_{i-1} = d_{i-1}$$\n\n即：**一次操作等价于差分数组上交换相邻的 $d_{i-1}$ 与 $d_i$**。\n\n**合法条件翻译**：$a_{i-1}$ 与 $a_{i+1}$ 同奇偶 $\\iff$ $d_{i-1} + d_i$ 为偶 $\\iff$ $d_{i-1}$ 与 $d_i$ 同奇偶。\n\n于是问题完全等价于：**每次可以交换差分数组中相邻且奇偶性相同的两项（任意多次），求能得到的最优差分序列。**\n\n### 第三步：可达状态刻画——极大同奇偶连续段\n\n**不变量**：交换双方同奇偶，交换后每个位置上数的奇偶不变，所以差分的\"逐位奇偶模式\"永不改变。据此把差分切成若干**极大同奇偶连续段**（段内奇偶全同、相邻段奇偶不同）：\n\n- 每段占据的位置集合永远固定（段边界两侧奇偶不同，永远无法交换越界）\n- 每段内数字的多重集守恒\n- 段内任意相邻两项同奇偶，交换随时合法；相邻交换足以实现段内任意排列（冒泡原理）\n\n**定理**：差分序列 $d'$ 可达，当且仅当它与 $d$ 的奇偶模式相同，且每个极大同奇偶段上的多重集相同。\n\n### 第四步：字典序最小化\n\n**a 的字典序比较 $\\iff$ d 的字典序比较**：设两个候选差分的第一个不同位置是 $k$，则两边前 $k-1$ 项前缀和相等，第 $k$ 处前缀和之差恰为 $d_k$ 与 $d'_k$ 之差。\n\n**逐段独立取升序**：各段占据的位置集合固定且互不重叠，互不干扰。固定多重集放到固定位置上，字典序最小的放法是**升序排序**。\n\n**最终结论**：把差分数组按极大同奇偶连续段切开，每段内部升序排序，再用固定的 $a_1$ 逐项累加还原，即为字典序最小的答案。\n\n### 存储约定\n\n本实现不另开差分数组，而是把原数组 A 原地改造成差分，并整体右移一位：\n\n| 阶段 | A[1] | A[2] 到 A[n] |\n|---|---|---|\n| 读入后 | $a_1$ | 原始 $a$ 值 |\n| 原地做差后 | $a_1$（不变） | 相邻差分 |\n| 分段排序后 | $a_1$（不变） | 每段升序的差分 |\n| 前缀和后 | $a_1$（原样输出） | 答案 $a_2$ 到 $a_n$ |\n\nA[1] 兼任哨兵与种子，A[0] 恒为 0 使做差时第一项无需特判。\n\n## 逐行代码解析\n\n### 原地做差（从右往左）\n\n```cpp\nfor (int i = n; i >= 1; i--) A[i] -= A[i - 1];\n```\n\n**方向是正确性的关键**：算位置 $i$ 的差分时，左邻位置必须还保存着原始 $a_{i-1}$。若从左往右做，左邻已被改成差分，算出的会变成二阶差分。从右往左则保证每个左邻仍是原始值。\n\n### 分段查找与排序\n\n```cpp\nfor (int i = 2; i <= n;)\n{\n    int j = i;\n    while (j + 1 <= n && ((A[i] & 1) == (A[j + 1] & 1))) j++;\n    sort(A.begin() + i, A.begin() + j + 1);\n    i = j + 1;\n}\n```\n\n- **起点是下标 2 而非 1**：A[1] 是冻结的 $a_1$，绝不能卷进排序\n- **判奇偶用位运算 `& 1`**：C++ 中负数取模得负，用 `% 2 == 1` 会把两个负奇数误判为不同奇偶\n- **用段首元素代表整段奇偶**：段内必然全同，比较段首即可\n- 找到段 $[i, j]$ 后直接 `sort` 升序排序\n\n### 前缀和还原输出\n\n```cpp\nfor (int i = 1; i <= n; i++)\n{\n    A[i] += A[i - 1];\n    cout << A[i] << ' ';\n}\ncout << '\\n';\n```\n\n从 A[1]（即 $a_1$）出发，从左到右累加：位置 $k$ 输出 $a_1 + \\sum_{j=1}^{k-1} d_j$，即新的 $a_k$。每段步长和守恒保证了所有墙（含 $a_n$）自动回到原值。\n\n## 复杂度分析\n\n### 时间复杂度\n\n| 操作 | 复杂度 | 说明 |\n|---|---|---|\n| 原地做差 | $O(n)$ | 从右到左一次扫描 |\n| 找段 + 排序 | $O(n \\log n)$ | 所有段排序总量不超过对整个差分排一次序 |\n| 前缀和还原 | $O(n)$ | 从左到右一次扫描 |\n| **总计** | $O(n \\log n)$ | 排序主导 |\n\n### 空间复杂度\n\n| 数据 | 空间 | 说明 |\n|---|---|---|\n| 数组 A | $O(n)$ | 长度 $n+1$，原地四用 |\n| **总计** | $O(n)$ | 线性空间 |\n\n## 常见陷阱与注意事项\n\n1. **做差方向必须从右往左**\n   - 若从左往右做，左邻已被改成差分，算出的变成二阶差分，全盘皆错\n\n2. **找段起点是下标 2 而非 1**\n   - A[1] 是冻结的 $a_1$，卷进排序会破坏端点不变性\n\n3. **判奇偶必须用位运算 `& 1`**\n   - C++ 中 `-3 % 2` 得 `-1`，用 `% 2 == 1` 会把负奇数误判为\"非奇数\"\n   - `(x & 1)` 对正负数都正确：奇数得 1，偶数得 0\n\n4. **全程使用 `long long`**\n   - 差分绝对值可达 $2 \\times 10^9$（超出 `int` 范围）\n   - 前缀和累计可达 $10^{14}$ 量级\n\n5. **排序对象是差分，不是原数组**\n   - 划分依据是差分的数值奇偶，与符号、大小无关（$-3$ 与 $5$ 同为奇数，$0$ 与 $-8$ 同为偶数）\n\n6. **划分一次即可，不需要迭代重找**\n   - 段内元素本来就同奇偶，排序只是段内置换，不改变任何位置上的奇偶，段边界不变\n\n7. **不能在原数组上直接按逐位奇偶贪心**\n   - 反例：$a = (0, 1, 4)$，差分 $(1, 3)$ 全奇一段。$a_2$ 只能取 1 或 3，并非任意奇数。真正的约束是\"段的多重集\"，必须先转差分再分段\n\n## 对比与扩展\n\n| 方法 | 时间 | 空间 | 特点 |\n|---|---|---|---|\n| **差分分段排序（本算法）** | $O(n \\log n)$ | $O(n)$ | 差分转化 + 分段独立排序 |\n| 暴力 BFS | $O(\\text{状态数})$ | 指数级 | 枚举所有可达状态，不可行 |\n| 原数组逐位贪心 | $O(n)$ | $O(n)$ | 似是而非，无法处理段的多重集约束 |\n\n### 扩展\n\n- **差分思想的应用**：将\"修改一个值\"转化为\"交换两个差分\"，是差分数组的经典技巧。类似思想可用于区间操作、回文构造等问题\n- **奇偶不变量**：许多操作问题的合法性条件可以转化为奇偶约束，进而转化为\"同奇偶连续段内自由排列\"的模型\n- **字典序最小化**：固定多重集的字典序最小排列就是升序排序，这一结论在许多贪心问题中反复出现"
    },
    {
        "id": "prob-cero-array",
        "title": "Cero 的数组（构造序列的最长上升子序列）",
        "oj": "",
        "problemId": "",
        "difficulty": "困难",
        "category": "动态规划",
        "tags": [
            "动态规划",
            "LIS",
            "树状数组",
            "离散化",
            "构造转化",
            "计数"
        ],
        "timeComplexity": "O(n log n)",
        "spaceComplexity": "O(n)",
        "codePath": "题目专辑\\动态规划\\Cero的数组\\源.cpp",
        "content": "## 题目描述\n\nMarton 的朋友 Cero 有一个由 $N$ 个正整数组成的数组。\n\n首先 Cero 会在黑板上写下这个数组中的第一个数字。接下来他会在之前写下的所有数的左边或者右边写下一个数字。重复以上操作得到一个序列。\n\n请注意，根据上述方法构造出的两个序列相同当且仅当每一个数字写下的顺序完全相同。例如，`1,1` 可能和 `1,1` 不同——前者的第二个数在第一个数的左边，后者的第二个数在第一个数的右边。\n\n求这些数组成的所有序列中，最长严格递增子序列长度的最大值 $M$，以及所有最长严格递增子序列长度等于 $M$ 的序列中，最长严格递增子序列个数的总和。考虑到答案可能很大，只需输出对 $10^9 + 7$ 取模的值。\n\n### 输入格式\n\n第一行包含一个正整数 $N$。\n\n接下来的一行包含 $N$ 个正整数，表示按顺序给出的这个数组的各个元素。\n\n### 输出格式\n\n仅一行，输出最长子序列长度以及总个数模上 $10^9 + 7$ 的值。\n\n### 数据规模与约定\n\n- 对于 30% 的数据，满足 $N \\le 20$\n- 对于 50% 的数据，满足 $N \\le 10^3$\n- 对于 100% 的数据，满足 $N \\le 2 \\times 10^5$，数组中的每个元素 $\\le 10^9$\n\n## 解题思路\n\n### 第一步：构造转化（本题最核心的难点）\n\n题目描述的构造过程是：先写第一个数，之后每个数只能放在当前序列的**最左端或最右端**。直接枚举所有可能的序列是不可行的（共有 $2^{n-1}$ 种），必须找到等价的数学刻画。\n\n**关键观察**：每个可能的序列，都可以看作是从原序列中选择一个子序列，将该子序列**翻转**后拼接到剩余部分的左侧。\n\n```\n原序列:       A1  A2  A3  A4  A5  A6  A7\n                        ↓\n选择子序列:        A2     A4     A6  (选中的元素按原序)\n                        ↓\n翻转子序列:   A6     A4     A2         (逆序)\n                        ↓\n拼接得到:     A6  A1  A4  A3  A2  A5  A7\n              ↑↑↑↑↑                    ↑↑↑↑↑\n              翻折过来的部分              未翻折的部分\n```\n\n换句话说，每个可能的序列 = 一个\"翻转前缀\" + 一个\"原始后缀\"。翻转前缀是原序列某个子序列的逆序，原始后缀是剩下的元素按原序排列。\n\n### 第二步：LIS 结构分析\n\n有了上述转化，我们分析可能序列的 LIS（最长严格上升子序列）结构：\n\n- 翻转前缀部分（从原序列中选出的子序列的逆序）在原序列中是**严格下降**的（因为逆序后上升 = 原序下降）\n- 原始后缀部分在原序列中是**严格上升**的\n- 一个可能序列的 LIS，由\"翻转前缀中贡献的上升段\"和\"原始后缀中贡献的上升段\"拼接而成\n\n更精确地说：对于原序列的某个位置 $i$，可能序列的 LIS 可以表示为：\n\n- 一个以 $i$ 结尾（向左方向）的**严格下降子序列**（贡献翻转前缀部分）\n- 一个以 $i$ 开头（向右方向）的**严格上升子序列**（贡献原始后缀部分）\n- 两段在位置 $i$ 处衔接（$i$ 同时属于两段，因此长度减 1）\n\n因此，最长 LIS 长度为：\n\n$$M = \\max_{i=1}^{n} \\left( \\text{len}_a[i] + \\text{len}_b[i] - 1 \\right)$$\n\n其中：\n- $\\text{len}_a[i]$ = 以 $i$ 结尾的、向左方向的最长严格下降子序列长度（即在原序列中从 $i$ 向左看，$A[i]$ 能接在哪些比它大的元素后面）\n- $\\text{len}_b[i]$ = 以 $i$ 开头的、向右方向的最长严格上升子序列长度\n\n### 第三步：方案数计算\n\n对于每个满足 $\\text{len}_a[i] + \\text{len}_b[i] - 1 = M$ 的位置 $i$，设 $\\text{cnt}_a[i]$ 和 $\\text{cnt}_b[i]$ 分别为对应方向的方案数，则：\n\n$$\\text{cnt}(i) = \\text{cnt}_a[i] \\times \\text{cnt}_b[i] \\times 2^{n - M}$$\n\n其中 $2^{n-M}$ 的含义是**不在 LIS 中的元素**的放置方式：\n\n**情况 1：$A_1$ 属于 LIS**\n\n- 不在 LIS 中的元素有 $n - M$ 个\n- 每个元素可以翻到左边或不翻（2 种选择）\n- 方案数：$2^{n-M}$\n\n**情况 2：$A_1$ 不属于 LIS**\n\n- 不在 LIS 中的元素有 $n - M$ 个（包括 $A_1$）\n- $A_1$ 必须翻到左边（只有 1 种选择，因为它不在 LIS 中却必须出现在翻转前缀中）\n- 其他 $n - M - 1$ 个元素各有 2 种选择\n- 衔接元素（同时属于两段的那个元素）不是 $A_1$，所以它可以跟着任一序列，翻不翻都可以，额外乘以 2\n- 方案数：$1 \\times 2^{n-M-1} \\times 2 = 2^{n-M}$\n\n两种情况方案数相同，均为 $2^{n-M}$，因此统一乘以 $2^{n-M}$ 即可。\n\n> **注意**：需要去重——同一个位置 $i$ 可能被 $\\text{cnt}_a$ 和 $\\text{cnt}_b$ 以不同方式计入，但由于我们按位置 $i$ 遍历，且 $i$ 是 LIS 的衔接点（唯一确定），因此不存在重复计数。\n\n### 第四步：树状数组优化\n\n求 $\\text{len}_a$、$\\text{cnt}_a$、$\\text{len}_b$、$\\text{cnt}_b$ 时，不能使用贪心+二分的 LIS 做法（因为贪心只维护一个最优解，无法统计方案数）。应使用**树状数组**维护\"以值 $v$ 结尾的最长长度及方案数\"。\n\n- **正序树状数组**（求向右的 LIS）：从右向左扫描，查询 $< A[i]$ 的最大长度及方案数\n- **倒序树状数组**（求向左的 LDS）：从右向左扫描，查询 $> A[i]$ 的最大长度及方案数（通过值域翻转实现）\n\n由于值域可达 $10^9$，需要先**离散化**。\n\n## 逐行代码解析\n\n### 树状数组结构\n\n```cpp\nstruct treearr\n{\n    vector<int> len;    // len[idx] = 树状数组位置 idx 处维护的最大长度\n    vector<int> cnt;    // cnt[idx] = 对应的方案数\n    int sz, ans_len, ans_cnt;\n```\n\n这个树状数组同时维护两个信息：**最大长度**和**对应的方案数**。与普通树状数组不同，`add` 操作不是简单累加，而是\"取 max 并合并计数\"。\n\n```cpp\n    void add(int idx, int num, int t_cnt)\n    {\n        if (num > len[idx]) len[idx] = num, cnt[idx] = t_cnt;\n        else if (num == len[idx]) cnt[idx] = (cnt[idx] + t_cnt) % mod;\n    }\n```\n\n- 如果新来的 `num` 更大，直接覆盖，方案数替换为 `t_cnt`\n- 如果 `num` 等于当前最大，方案数累加\n\n```cpp\n    void insert(int idx, int num, int t_cnt)\n    {\n        while (idx <= sz)\n        {\n            add(idx, num, t_cnt);\n            idx += lowbit(idx);\n        }\n    }\n```\n\n标准树状数组单点更新：沿 `idx += lowbit(idx)` 向上更新所有覆盖该位置的节点。\n\n```cpp\n    void get(int idx)\n    {\n        ans_len = 0, ans_cnt = 0;\n        while (idx)\n        {\n            if (len[idx] > ans_len)\n            {\n                ans_len = len[idx];\n                ans_cnt = cnt[idx];\n            }\n            else if (len[idx] == ans_len) ans_cnt = (ans_cnt + cnt[idx]) % mod;\n            idx -= lowbit(idx);\n        }\n    }\n```\n\n前缀查询：沿 `idx -= lowbit(idx)` 向下查询 $[1, \\text{idx}]$ 范围内的最大长度及方案数。同样地，遇到更长的就替换，遇到等长的就累加。\n\n### 主函数\n\n```cpp\nint main()\n{\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr); cout.tie(nullptr);\n    int n;\n    cin >> n;\n    vector<int> fac(n + 1);\n    fac[0] = 1;\n    for (int i = 1; i <= n; i++) fac[i] = (fac[i - 1] << 1) % mod;\n```\n\n预处理 $2$ 的幂次表 `fac[i]`（即 $2^i \\bmod \\text{mod}$），用于后续方案数计算中的 $2^{n-M}$。\n\n```cpp\n    vector<int> A(n + 1);\n    for (int i = 1; i <= n; i++) cin >> A[i];\n    vector<int> tmp(A);\n    sort(tmp.begin() + 1, tmp.end());\n    tmp.erase(unique(tmp.begin() + 1, tmp.end()), tmp.end());\n```\n\n**离散化**：排序 + 去重，将值域从 $[1, 10^9]$ 压缩到 $[1, m]$（$m$ 为不同值的个数）。离散化后才能用树状数组维护。\n\n```cpp\n    treearr arr_a(tmp.size());  // 正序：维护向右的严格上升子序列\n    treearr arr_b(tmp.size());  // 倒序：维护向左的严格下降子序列\n    vector<int> len_a(n + 1), cnt_a(n + 1);\n    vector<int> len_b(n + 1), cnt_b(n + 1);\n```\n\n两个树状数组分别维护两个方向：\n- `arr_a`：查询 $< A[i]$ 的最大长度（向右 LIS），通过值域翻转实现\"严格小于\"\n- `arr_b`：查询 $< A[i]$ 的最大长度（向左 LDS），直接用原值\n\n```cpp\n    for (int i = n; i >= 1; --i)\n    {\n        A[i] = lower_bound(tmp.begin() + 1, tmp.end(), A[i]) - tmp.begin();\n        int a = tmp.size() - A[i] + 1, b = A[i];\n```\n\n**从右向左扫描**：先离散化 $A[i]$，然后计算两个查询位置：\n- `a = m - A[i] + 1`：值域翻转，使得查询 `[1, a-1]` 等价于查询\"值 $> A[i]$\"（向左的严格下降）\n- `b = A[i]`：原值，查询 `[1, b-1]` 等价于查询\"值 $< A[i]$\"（向右的严格上升）\n\n```cpp\n        arr_a.get(a - 1);\n        len_a[i] = arr_a.ans_len + 1;\n        cnt_a[i] = arr_a.ans_cnt ? arr_a.ans_cnt : 1;\n        arr_a.insert(a, len_a[i], cnt_a[i]);\n```\n\n**求向左的严格下降子序列**（即翻转前缀的贡献）：\n- `arr_a.get(a - 1)`：查询值 $> A[i]$ 的元素中，最大长度及方案数\n- `len_a[i] = ans_len + 1`：在之前最优解基础上延伸 1\n- `cnt_a[i]`：若查询结果为空（`ans_cnt == 0`），说明 $A[i]$ 是第一个，方案数为 1\n- `arr_a.insert(a, ...)`：将当前元素插入树状数组\n\n```cpp\n        arr_b.get(b - 1);\n        len_b[i] = arr_b.ans_len + 1;\n        cnt_b[i] = arr_b.ans_cnt ? arr_b.ans_cnt : 1;\n        arr_b.insert(b, len_b[i], cnt_b[i]);\n    }\n```\n\n**求向右的严格上升子序列**（即原始后缀的贡献）：同理，用原值 $b = A[i]$ 查询。\n\n```cpp\n    int ans_len = 0, ans_cnt = 0;\n    for (int i = 1; i <= n; i++)\n    {\n        int len = len_a[i] + len_b[i] - 1;\n        int cnt = (((((long long)cnt_a[i] * cnt_b[i]) % mod) * fac[n - len]) % mod);\n        if (len > ans_len) ans_cnt = cnt, ans_len = len;\n        else if (len == ans_len) ans_cnt = (ans_cnt + cnt) % mod;\n    }\n    cout << ans_len << ' ' << ans_cnt;\n    return 0;\n}\n```\n\n**合并答案**：遍历每个位置 $i$，计算拼接后的 LIS 长度 `len = len_a[i] + len_b[i] - 1`（减 1 因为位置 $i$ 被两段各算了一次），方案数 `cnt = cnt_a[i] * cnt_b[i] * 2^(n-len)`。\n\n取所有位置中长度最大的方案数之和。注意乘法用 `long long` 中间转换避免溢出。\n\n## 复杂度分析\n\n### 时间复杂度\n\n| 操作 | 复杂度 | 说明 |\n|---|---|---|\n| 离散化（排序+去重） | $O(n \\log n)$ | 对 $n$ 个元素排序 |\n| 逆序扫描（求 len/cnt） | $O(n \\log n)$ | 每个位置 2 次树状数组查询 + 2 次更新，每次 $O(\\log n)$ |\n| 合并答案 | $O(n)$ | 遍历所有位置 |\n| **总计** | $O(n \\log n)$ | 离散化 + 树状数组主导 |\n\n### 空间复杂度\n\n| 数据 | 空间 | 说明 |\n|---|---|---|\n| 离散化数组 `tmp` | $O(n)$ | 去重后的值域 |\n| 两个树状数组 | $O(n)$ | 各 $n+1$ 大小的 `len` 和 `cnt` |\n| DP 数组 | $O(n)$ | `len_a`, `cnt_a`, `len_b`, `cnt_b` 各 $n+1$ |\n| 幂次表 `fac` | $O(n)$ | 预处理 $2^0$ 到 $2^n$ |\n| **总计** | $O(n)$ | 线性空间 |\n\n## 常见陷阱与注意事项\n\n1. **构造转化是本题核心**\n   - 题目描述的\"左右放数\"操作等价于\"选子序列翻转后拼接\"\n   - 这个转化是整道题的突破口，没有这个观察无法继续\n\n2. **不能用贪心+二分求 LIS**\n   - 贪心+二分只维护最优解的末尾值，无法统计方案数\n   - 必须用树状数组同时维护最大长度和方案数\n\n3. **值域翻转技巧**\n   - 求严格下降子序列时，将值 $v$ 映射为 $m - v + 1$，这样\"查询 $> v$\"就变成了\"查询 $< (m-v+1)$\"，统一用树状数组的前缀查询\n\n4. **$A_1$ 的特殊性**\n   - $A_1$ 是第一个被写入的数，它决定了\"翻折\"的起点\n   - 方案数推导中 $A_1$ 是否属于 LIS 影响中间过程，但最终两种情况方案数相同（均为 $2^{n-M}$）\n\n5. **离散化必须去重**\n   - `sort` + `unique` 保证相同值映射到同一位置\n   - 严格上升/下降要求\"不等于\"，离散化后用 `b - 1` / `a - 1` 查询即可\n\n6. **乘法溢出**\n   - `cnt_a[i] * cnt_b[i]` 两个 `int` 相乘可能溢出，必须先转 `long long`\n   - 代码中 `((((long long)cnt_a[i] * cnt_b[i]) % mod) * fac[n - len]) % mod` 逐步取模\n\n7. **从右向左扫描**\n   - 因为我们需要\"以 $i$ 结尾向左\"和\"以 $i$ 开头向右\"的信息，而树状数组查询的是\"之前已插入的元素\"\n   - 从右向左扫描时，已插入的是 $i$ 右边的元素，查询结果正好是\"以 $i$ 为起点的向右信息\"\n\n## 对比与扩展\n\n| 方法 | 时间 | 空间 | 能否统计方案数 | 适用场景 |\n|---|---|---|---|---|\n| 朴素 DP | $O(n^2)$ | $O(n)$ | 能 | 小数据，$n \\le 5000$ |\n| 贪心+二分 | $O(n \\log n)$ | $O(n)$ | 不能 | 只需长度，不需方案数 |\n| **树状数组（本算法）** | $O(n \\log n)$ | $O(n)$ | **能** | 需要长度+方案数 |\n| 线段树 | $O(n \\log n)$ | $O(n)$ | 能 | 同树状数组，常数稍大 |\n\n### 扩展\n\n- **LIS 方案数问题**：本题的树状数组技巧可直接用于\"求 LIS 长度及方案数\"的标准问题\n- **LCS 方案数**：类似的\"维护最大值+计数\"思路也可用于最长公共子序列方案数\n- **区间 DP 中的计数**：许多需要同时维护最优值和方案数的 DP 问题都可以借鉴这种\"树状数组同时维护 len 和 cnt\"的技巧"
    },
    {
        "id": "prob-cf-binary-bubble-sort-inversions",
        "title": "F. Binary Bubble Sort Inversions — 01数组冒泡逆序对计数（懒标记水位模型）",
        "oj": "Codeforces",
        "problemId": "F",
        "difficulty": "困难",
        "category": "数据结构",
        "tags": [
            "逆序对",
            "01数组",
            "冒泡排序",
            "懒标记",
            "水位模型",
            "前缀和",
            "吸收态",
            "O(n)"
        ],
        "timeComplexity": "O(n)",
        "spaceComplexity": "O(n)",
        "codePath": "题目专辑\\数据结构\\BinaryBubbleSortInversions\\代码.cpp",
        "content": "## 题目描述\n\n给定一个仅含 $0$ 和 $1$ 的数组 $a_1, a_2, \\ldots, a_n$ 和一个长度为 $n$ 的二进制字符串 $s$。依次执行 $n$ 个操作，第 $i$ 个操作：\n\n- 若 $s_i = 1$，对 $a$ 执行一次**正向冒泡**：从左到右扫描，若 $a_i > a_{i+1}$ 则交换\n- 若 $s_i = 0$，对 $a$ 执行一次**反向冒泡**：从右到左扫描，若 $a_i > a_{i+1}$ 则交换\n\n对 $n+1$ 个状态（初始状态 + 每次操作后的状态），分别输出数组 $a$ 的**逆序对数**。\n\n> **逆序对**：满足 $1 \\le i < j \\le m$ 且 $c_i > c_j$ 的下标对 $(i, j)$ 的个数。\n\n### 正向冒泡示例\n\n对 $[1, 1, 0]$ 执行正向冒泡：$[1, 1, 0] \\to [1, 1, 0]$（$1 \\le 1$，不换）$\\to [1, 0, 1]$（$1 > 0$，交换）。结果：$[1, 0, 1]$。\n\n### 反向冒泡示例\n\n对 $[1, 1, 0]$ 执行反向冒泡：从右往左，$i = 1$：$a_1 = 1 > a_2 = 1$? 否，不换；然后看 $i = 0$... 实际上反向冒泡从 $i = m-1$ 到 $i = 1$：$[1, 1, 0] \\to [1, 0, 1]$（$a_2 = 1 > a_3 = 0$，交换）$\\to [0, 1, 1]$（$a_1 = 1 > a_2 = 0$，交换）。结果：$[0, 1, 1]$。\n\n## 输入格式\n\n- 第一行：$t$（$1 \\le t \\le 10^4$）— 测试组数\n- 每组：\n  - 第一行：$n$（$1 \\le n \\le 5 \\times 10^5$）\n  - 第二行：$a_1, a_2, \\ldots, a_n$（$a_i \\in \\{0, 1\\}$）\n  - 第三行：$s$（$s_i \\in \\{0, 1\\}$，$|s| = n$）\n\n保证所有测试组的 $n$ 之和不超过 $5 \\times 10^5$。\n\n## 输出格式\n\n每组输出 $n+1$ 个空格分隔的整数，第 $i$ 个表示前 $i-1$ 次操作后的逆序对数。\n\n## 样例\n\n### 样例输入\n\n```\n1\n6\n1 1 0 0 1 0\n1001\n```\n\n### 样例输出\n\n```\n7 4 2 0 0\n```\n\n### 样例推演\n\n初始数组 $a = [1,1,0,0,1,0]$，$s = [1,0,0,1]$：\n\n| 步骤 | 操作 | 数组变化 | 逆序对 | 说明 |\n|------|------|---------|--------|------|\n| 初始 | — | $[1,1,0,0,1,0]$ | 7 | 3 个 0，各自前面有 2, 2, 3 个 1 |\n| 1 | 正向 | $[1,0,0,1,0,1]$ | 4 | 最左 1 冲到队尾，穿过 3 个 0 |\n| 2 | 反向 | $[0,1,0,0,1,1]$ | 2 | 最右 0 搬到队首，减去最大账 2 |\n| 3 | 正向 | $[0,0,0,1,1,1]$ | 0 | 水位再涨 1，剩余 2 根柱子全没顶 |\n| 4 | 反向 | $[0,0,0,1,1,1]$ | 0 | 已有序，空操作 |\n\n## 解题思路\n\n### 核心建模：逆序对 = 每个 0 的\"账\"\n\n01 数组中，逆序对 $=$ 前面的 $1$ 与后面的 $0$ 组成的对。按 $0$ 归账：第 $i$ 个 $0$ 前面有几个 $1$，它就贡献几对。\n\n设 $d_i$ 为第 $i$ 个 $0$ 前面的 $1$ 的个数，则：\n\n$$\\text{inv} = \\sum_{i=1}^{\\text{cnt0}} d_i$$\n\n**建表**：一遍扫描，`cnt1` 遇 $1$ 加一、遇 $0$ 记账（`inv += cnt1; count[cnt1]++; A.push_back(cnt1)`）。\n\n**两条免费性质**（省掉排序和堆）：\n\n1. `cnt1` 只增不减 $\\Rightarrow$ $d$ 沿位置非降 $\\Rightarrow$ $A[1 \\ldots \\text{cnt0}]$ 天然有序\n2. \"当前最大的账\"永远是幸存段的最后一根 $A[k]$\n\n### 正向操作（$s_i = 1$）：水位 +1\n\n正向冒泡使最左的 $1$ 一路向右穿过挡道的 $0$。$0$ 在最左 $1$ 右边 $\\Leftrightarrow$ 它的 $d \\ge 1$。每被穿过一次，那个 $0$ 前面恰好少一个 $1$ $\\Rightarrow$ 所有 $d \\ge 1$ 的账各减 $1$，$d = 0$ 的不动。\n\n复合 $T$ 次后：$d \\to \\max(d - T, 0)$，减到 $0$ 封底不为负。\n\n### 反向操作（$s_i = 0$）：删掉最大账\n\n反向冒泡使最右的 $0$ 搬到队首。搬家只挪这一根 $0$，其他元素相对次序不变 $\\Rightarrow$ 其他 $0$ 的账不动；搬家这根前面变成没有 $1$，账 $\\to 0$。而 $d$ 沿位置非降，最右 $0$ 恰好是账最大的 $\\Rightarrow$ 反向 $=$ 删掉当前最大的一笔账。\n\n### 懒标记（水位模型）\n\n逐个减账是每次 $O(n)$。改成记账不记账面：\n\n| 变量 | 含义 |\n|------|------|\n| `lazy1 = T` | 正向操作总次数 $=$ 水位 |\n| `lazy0` | 已拆掉的柱子数（反向操作次数） |\n| `A` | 冻结的账本，柱子真实高度 $= \\max(A_i - T, 0)$ |\n| 幸存柱 | $A[1 \\ldots k]$，$k = \\text{cnt0} - \\text{lazy0}$ |\n\n**总不变式**（一切正确性的核心）：\n\n$$\\text{inv} = \\sum_{i=1}^{k} \\max(A_i - \\text{lazy1}, 0)$$\n\n且它等于真实数组的逆序对数。柱子同步沉降（都减同一个 $T$）、拆的又总是最高的，所以幸存者永远是前缀。\n\n**正向**：水位 $+1$；`inv` 减去\"涨水前还露在水面上的幸存柱数\"（它们各降 $1$）。\n\n**反向**：拆掉 $A[k]$；`inv` 减去它当时的露出高度 $= A[k] - \\text{lazy1}$。\n\n### 吸收态：inv = 0（本代码的灵魂）\n\n$\\text{inv} = 0$ $\\Leftrightarrow$ 全部幸存柱真实高度为 $0$ $\\Leftrightarrow$ 数组已排成 $00 \\cdots 011 \\cdots 1$。\n\n$T$ 只涨、$k$ 只减 $\\Rightarrow$ 一旦归零永远为零。有序数组上两种冒泡物理上都是空操作。代码用 `if (inv)` 罩住整个操作处理，归零后全部跳过。\n\n**附赠安全保证**：$\\text{inv} > 0$ $\\Rightarrow$ 幸存非空 $\\Rightarrow$ $\\text{idx} \\ge 1$，永不越界。\n\n### 新产生的 0 不用记账\n\n正向操作后数组尾部可能新增 $0$（最左 $1$ 冲到队尾变成... 实际上 1 变成在队尾，0 不会新增）。反向操作后队首新增 $0$。① 它账值 $0$，水位只涨，真实值永远 $0$；② 反向删的永远是最大值，账 $0$ 的永远轮不到它。\n\n## 逐行代码解析\n\n### 建表阶段\n\n```cpp\nlong long inv = 0;\nint cnt0 = 0, cnt1 = 0;\nvector<int> count(n + 1);\nvector<long long> A(1);\n\nfor (int i = 1; i <= n; i++)\n{\n    if (a[i] == '0')\n    {\n        cnt0++;\n        inv += cnt1;\n        count[cnt1]++;\n        A.emplace_back(cnt1);\n    }\n    else cnt1++;\n}\n\nfor (int i = 1; i <= n; i++) count[i] += count[i - 1];\n```\n\n| 行 | 代码 | 解析 |\n|----|------|------|\n| 1 | `long long inv = 0;` | 逆序对总数，必须 `long long`（上界 $n^2/4 \\approx 6 \\times 10^{10}$） |\n| 2 | `int cnt0 = 0, cnt1 = 0;` | 0 和 1 的计数器 |\n| 3 | `vector<int> count(n + 1);` | `count[v]` 记录 $d = v$ 的 0 的个数，后续做前缀和 |\n| 4 | `vector<long long> A(1);` | 账本数组，`A[0] = 0` 作为哨兵 |\n| 5-12 | `for (int i = 1; i <= n; i++)` | 一遍扫描建表 |\n| 7 | `inv += cnt1;` | 遇到 0，它前面有 `cnt1` 个 1，贡献 `cnt1` 对 |\n| 8 | `count[cnt1]++;` | 记录 $d = \\text{cnt1}$ 的 0 多了一个 |\n| 9 | `A.emplace_back(cnt1);` | 把账值压入账本（天然有序） |\n| 14 | `count[i] += count[i - 1];` | 前缀和：`count[v]` 变为 $d \\le v$ 的 0 的总个数 |\n\n### 操作阶段\n\n```cpp\nint lazy0 = 0, lazy1 = 0;\ncout << inv << ' ';\nfor (int i = 1; i <= n; i++)\n{\n    if (inv)\n    {\n        if (s[i] == '1')\n        {\n            lazy1++;\n            int ct = cnt0 - count[lazy1 - 1] - lazy0;\n            if (ct > 0) inv -= ct;\n        }\n        else\n        {\n            lazy0++;\n            int idx = cnt0 - lazy0 + 1;\n            if (A[idx] - lazy1 > 0) inv -= A[idx] - lazy1;\n        }\n    }\n    cout << inv << ' ';\n}\n```\n\n| 行 | 代码 | 解析 |\n|----|------|------|\n| 1 | `int lazy0 = 0, lazy1 = 0;` | 两个懒标记：水位和拆柱数 |\n| 2 | `cout << inv << ' ';` | 输出初始逆序对数 |\n| 4 | `if (inv)` | **吸收态卫兵**：inv = 0 时整个操作跳过 |\n| 6 | `lazy1++;` | 正向：水位先 +1 |\n| 7 | `int ct = cnt0 - count[lazy1 - 1] - lazy0;` | 计算涨水前还露面的幸存柱数 |\n| 8 | `if (ct > 0) inv -= ct;` | 每根露面柱各降 1，inv 减去露面数量 |\n| 10-11 | `lazy0++; int idx = cnt0 - lazy0 + 1;` | 反向：先拆一根，`idx` 指向被拆柱（拆之前的幸存数 $k$） |\n| 12 | `if (A[idx] - lazy1 > 0) inv -= A[idx] - lazy1;` | 被拆柱的露出高度 $=$ inv 减少量 |\n\n**正向分支 `ct` 逐项拆解**：\n\n$$\\text{ct} = \\text{cnt0} - \\text{count}[T_{\\text{旧}}] - \\text{lazy0}$$\n\n- $\\text{cnt0} - \\text{count}[T_{\\text{旧}}]$：全体 0 中 $d > T_{\\text{旧}}$ 的根数\n- $- \\text{lazy0}$：去掉已拆的\n\n**为什么 `count` 不用做\"幸存修正\"**：进入分支前提 $\\text{inv} > 0$ $\\Rightarrow$ 最大幸存账 $A[k] > T_{\\text{旧}}$ $\\Rightarrow$ 已拆的（都 $\\ge A[k]$）全 $> T_{\\text{旧}}$，一根都不在 $\\text{count}[T_{\\text{旧}}]$ 里。\n\n**反向分支 `idx` 推导**：拆之前 $k = \\text{cnt0} - \\text{lazy0}_{\\text{旧}} = \\text{cnt0} - \\text{lazy0}_{\\text{新}} + 1$，正是被拆那根的下标。`A[idx]` 是账面原值，`A[idx] - lazy1` 是它被拆那一刻的真实高度。\n\n## 复杂度分析\n\n| 维度 | 复杂度 | 说明 |\n|------|--------|------|\n| 时间 | $O(n)$ | 建表一遍扫描 $O(n)$ + 前缀和 $O(n)$ + 每操作 $O(1)$ |\n| 空间 | $O(n)$ | `count` 数组 + `A` 数组 + 输入数组 |\n\n**关键规模分析**：\n- `inv` 上界 $n^2 / 4 \\approx 6 \\times 10^{10}$（$n = 5 \\times 10^5$），必须用 `long long`\n- `A` 数组存储 `long long` 类型（虽然 $d_i \\le n$ 用 `int` 足够，但为安全用 `long long`）\n- `count` 数组用 `int` 足够（计数不超过 $n$）\n\n## 常见陷阱与注意事项\n\n### 1. A 不能预开 $n+1$ 个 0 再 `emplace_back`\n\n如果写成 `vector<long long> A(n + 1, 0)` 再 `emplace_back`，数据整体错位（前面多出 $n+1$ 个无意义的 0）。正确姿势：`A(1)` 只放哨兵 $A[0] = 0$，且 `A` 不做前缀和（它要取单值）。\n\n### 2. $d = 0$ 的 0 也要入 `count` 和 `A`\n\n贡献虽恒为 $0$，但必须占坑，否则位置计数全错。`count[0]` 记录了 $d = 0$ 的 0 的个数，它们在水位 $0$ 时就\"没顶\"，但数量必须正确。\n\n### 3. `lazy0` 只能数\"拆成功\"的反向操作\n\n归零后的反向操作是空操作，指针不能虚移。本版用 `if (inv)` 卫兵等价实现：归零后 `lazy0` 不再增加。\n\n### 4. 数学空操作 $\\ne$ 代码空操作\n\n有序后若照跑，`idx` 会一路左移出界，`A[idx]` 读到垃圾值再污染 `inv`（WA/RE 的真凶）。卫兵 `if (inv)` 从根上断掉这条路。\n\n### 5. `count` 做了前缀和，`A` 没做\n\n两个查询工具的语义不同：\n\n| 结构 | 含义 | 用途 |\n|------|------|------|\n| `count[v]`（已前缀和） | 全体 0 中 $d \\le v$ 的根数 | $O(1)$ 知道水位下没顶多少根 |\n| `A[0 \\ldots \\text{cnt0}]` | $A[0] = 0$ 哨兵，$A[1 \\ldots \\text{cnt0}]$ 为 $d$ 原值 | 按下标取单值（注意它不做前缀和！） |\n\n### 6. `if (ct > 0)` 和 `if (A[idx] - lazy1 > 0)` 是冗余保险\n\n$\\text{inv} > 0$ 时必有 $\\text{ct} > 0$（正向）和 $A[\\text{idx}] > \\text{lazy1}$（反向），留着无害，作为安全冗余。\n\n### 7. 全 1 / 全 0 的退化输入\n\n全 1：`cnt0 = 0`，`inv = 0`，卫兵直接接管，不越界。全 0：`cnt1 = 0`，`inv = 0`，同理。无需特判。"
    },
    {
        "id": "prob-cf-diss-quack-array-game",
        "title": "D. diss_quack and Array Game — 01博弈·进位悬崖·批量折半优化",
        "oj": "Codeforces",
        "problemId": "D",
        "difficulty": "困难",
        "category": "数学",
        "tags": [
            "博弈论",
            "数论",
            "贪心",
            "popcount",
            "位运算",
            "剪枝",
            "进位悬崖",
            "批量折半",
            "枚举优化"
        ],
        "timeComplexity": "O(2^18 + T * 50n)",
        "spaceComplexity": "O(2^18 + n)",
        "codePath": "题目专辑\\数学\\DissQuackArrayGame\\源.cpp",
        "content": "## 题目描述\n\n给定一个长度为 $n$ 的正整数数组 $a$。Alice 和 Bob 用这个数组做游戏。\n\n**游戏开始前**，Alice 可以给任意元素加 1，每次加 1 消耗一步操作。加的次数不限。\n\n**游戏开始后**，Bob 和 Alice 轮流操作，Bob 先手：\n\n- **Bob 的回合**：选任意两个下标 $i, j$ 交换 $a_i$ 和 $a_j$（可以 $i = j$，即不动）\n- **Alice 的回合**：若 $a_1$ 是偶数，她找到最大的 $j \\le |a|$ 使得 $a_i$ 对所有 $i \\le j$ 都是偶数，然后对所有 $i \\le j$ 执行 $a_i := a_i / 2$；否则她执行 $a_1 := a_1 - 1$。如果任何元素变为 0，它自动从数组中删除。\n\n游戏在数组为空时结束。Alice 想最小化她的总操作数（初始加 1 的次数 + 游戏中她的回合数），Bob 想最大化。双方最优博弈下 Alice 的操作数是多少？\n\n### 题意统一表述\n\n将游戏重新表述为更清晰的形式：\n\n- **Alice 阶段**：可以给任意 $a_i$ 加 1，每加 1 花费 1 步，次数不限。目标是最小化总花费。\n- **Bob 阶段**：每一轮规则是**强制的**——只要当前还有偶数，这一轮就把**所有偶数同时折半**（奇数原地不动）；只有当剩下的数全是奇数时，这一轮才选一个数减 1。打到 0 的数退场。目标是全部变 0。\n\n**关键体会**：Bob 的流程几乎没有自由度——轮数由\"垫完之后的数组\"**唯一决定**。所以整个问题变成 Alice 如何垫数，让\"花费 + 轮数\"最小。\n\n## 输入格式\n\n- 第一行：$t$（$1 \\le t \\le 10^4$）— 测试组数\n- 每组：\n  - 第一行：$n$（$1 \\le n \\le 10^5$）— 数组长度\n  - 第二行：$a_1, a_2, \\ldots, a_n$（$1 \\le a_i \\le 10^5$）\n\n保证所有测试组的 $n$ 之和不超过 $10^5$。\n\n## 输出格式\n\n每组输出一个整数——Alice 在双方最优博弈下的操作数。\n\n## 样例\n\n### 样例验证\n\n以下用思路文档中的手算案例验证算法正确性：\n\n| 输入 | 输出 | 验证过程 |\n|------|------|---------|\n| `{3, 4}` | 5 | 最优在 $L=2$：垫 $3 \\to 4$（付 1），前 2 轮共同折半 $(4,4) \\to (2,2) \\to (1,1)$，之后各减 1 消场。总 $= 2 + (1 + g[1]) + (0 + g[1]) = 2 + 2 + 1 = 5$ |\n| `{15}` | 6 | $L=1$：付 1 垫到 16，$1 + 1 + g[8] = 6$（$L=2,3,4$ 都是 6，形成平台） |\n| `{1, 1}` | 2 | 全是奇数，批量无用，$L=0$：$1 + 1 = 2$ |\n| `{5, 5}` | 8 | $g[5] + g[5] = 4 + 4 = 8$；垫到 $(6,6)$ 反而 $2 + 7 = 9$ 更差 |\n\n## 解题思路\n\n### 一、单个数要打多少轮：g 表\n\n只有一个数 $x$ 时，它的操作序列是固定的：奇数只能减 1，偶数折半。定义：\n\n$$g[0] = 0, \\quad g[x] = \\begin{cases} g[x-1] + 1, & x \\text{ 为奇} \\\\ g[x/2] + 1, & x \\text{ 为偶} \\end{cases}$$\n\n闭式：\n\n$$g(x) = \\lfloor \\log_2 x \\rfloor + \\text{popcount}(x)$$\n\n直观理解：二进制里每个 1 都要付一次\"减 1\"（popcount 次），每降一个二进制位付一次\"折半\"（最高位位置 $= \\lfloor \\log_2 \\rfloor$ 次）。\n\n| $x$ | 1 | 2 | 3 | 4 | 5 | 7 | 8 | 12 | 15 | 16 |\n|-----|---|---|---|---|---|---|---|----|----|----|\n| $g$ | 1 | 2 | 3 | 3 | 4 | 5 | 4 | 5  | 7  | 5  |\n\n两个直接推论：\n\n1. $a_i \\le 10^5 < 2^{17}$，所以 $g \\le 16 + 17 = 33$。这是后面一切剪枝的**锚点**。\n2. 打表范围开 $2^{18}$：查询下标是\"垫完再移位\"的值，上界 $10^5 + 2^{17}$ 移位后不超过 $2^{17}$，开 $2^{18}$ 富余。\n\n### 二、保底方案（L = 0）\n\n什么都不垫，各打各的：\n\n$$\\text{ans}_0 = \\sum_{i} g(a_i)$$\n\n这是全局保底，也是后面论证\"超过 34 必弃\"的参照物。\n\n### 三、批量折半：把 L 轮\"团购\"出来\n\n规则里最值钱的一条：一轮能**同时折半所有偶数**。要想连续 $L$ 轮都享受这个团购，必须保证这 $L$ 轮里不出现奇数 $\\Leftrightarrow$ Alice 把每个数都垫成 $2^L$ 的倍数。\n\n**结构定理**（整题的骨架）：\n\n设 Alice 垫完得到 $a_i'$，前 $Q$ 轮是\"全员共同折半\"（第 $Q+1$ 轮起出现奇数），则：\n\n1. 前 $Q$ 轮全员都是偶数 $\\Leftrightarrow$ 所有 $a_i'$ 都是 $2^Q$ 的倍数\n2. $Q$ 轮之后进入\"逐个结算\"：全奇 $\\to$ 减 1 某个数 $\\to$ 它变偶 $\\to$ 下一轮只折它一个……每一轮至多只有一个数在动，且每个数的个人操作序列和它单独打时一模一样\n3. 总轮数 $= Q + \\sum_i g(a_i' \\gg Q)$，与结算顺序**无关**\n\n第 3 点成立的原因：逐个结算阶段每轮服务的恰好是一个数的\"个人脚本\"里的一步，脚本是确定的（奇减偶半），没人能插队共享，所以轮数就是各人脚本长度之和。\n\n于是答案结构定型：\n\n$$\\text{ans} = \\min_{L} \\left[ L + \\sum_i \\left( \\underbrace{c_i^1}_{\\text{垫付}} + \\underbrace{g\\!\\left((a_i + c_i^1) \\gg L\\right)}_{\\text{单打}} \\right) \\right]$$\n\n三个细节：\n\n- $L$ 只加一次（记为 $c^2 = L$），因为它是全员共享的团购轮，这是批量唯一的收益来源\n- 若垫到 $2^L$ 倍数后有的数折半仍是偶数，共同折分会自动延长——那笔账恰好等于更高一档 $L'$ 以相同垫付算出的值，被外层枚举覆盖\n- $L$ 的枚举范围 $0 \\sim 17$：$2^{17} > 10^5$，再高的档垫付就要超过 $2^{17} - 10^5 \\gg 33$，必被 $L=0$ 支配\n\n### 四、固定 L 后，每个数是一本独立小账\n\n选定 $L$ 后，数与数之间不再互相影响，每个数独立选垫付终点：\n\n- $c^1$：垫到某个 $2^L$ 的倍数（最近的是 $(k - a_i \\bmod k) \\bmod k$，$k = 2^L$）\n- 折半后剩 $(a_i + c^1) \\gg L$，单打花 $g$\n- 本数小账 $= c^1 + g(\\cdot)$，全局求和后 $+ L$\n\n注意终点不必是最近倍数——这就引出第五节。\n\n### 五、垫到最近倍数就够了吗？不——进位悬崖\n\n把 $g$ 拆成两项看涨价逻辑：\n\n- $\\lfloor \\log_2 \\rfloor$：数值翻倍才 +1，慢涨\n- popcount：遇到连续进位一次跌掉\"末尾连续 1 的个数\"，**骤跌**\n\n所以\"多垫几块钱\"反而更便宜的场景真实存在：\n\n| 数 | 二进制 | $g$ | 说明 |\n|----|--------|-----|------|\n| 15 | 01111 | 7 | — |\n| 16 | 10000 | 5 | 付 1 省 2 ✓ |\n| 62 | 111110 | 10 | — |\n| 64 | 1000000 | 7 | 付 2 省 3 ✓ |\n\n悬崖的本质：形如 $\\ldots x\\,0\\,\\underbrace{11\\ldots1}_{k} \\to \\ldots x\\,1\\,\\underbrace{00\\ldots0}_{k}$ 的进位，把末尾一串 1 收拢成一个 1。悬崖遍地都是，2 的幂只是\"popcount 归 1\"的最大号悬崖——比如 62 的最优落点是 64，而 30、46 这类数的就近悬崖（32、48）甚至不是 2 的幂。**悬崖位置没有公式可枚举，所以只能把窗口内的倍数格点挨个算一遍。**\n\n### 六、窗口为什么这么小：三条剪枝定理\n\n固定 $L$，从最近倍数开始按步长 $k$ 往上跳（每跳一格，移位后的值恰好 +1，多付 $k$ 块）。三个终止条件：\n\n**① 34 界（保底剪枝）**\n\n单数任何方案都有保底对照——$L=0$ 档最多 33 轮。所以\"额外垫付增量 $\\ge 34$\"的候选必败。形式化：被跳过的候选账 $\\ge 34 + g(\\cdot) \\ge 35$，而本档 $cnt = 0$ 的账 $\\le c^1 + 33$，同档比较差至少 2，弃之无悔。代码即 `if (k * cnt >= 34) break;`。\n\n**② 2 的幂封顶**\n\n移位后的值到达 2 的幂时 popcount 已见底为 1，$\\lfloor \\log_2 \\rfloor$ 只涨不跌，之后不存在更优落点，break。代码判的是原值 $t$，因为 $k \\mid t$ 且 $k$ 是 2 的幂，$t$ 是 2 的幂 $\\Leftrightarrow t \\gg L$ 是 2 的幂，等价。\n\n**③ 高档接管**\n\n本档窗口之外更远的悬崖，全部以更便宜的价格出现在某个更高 $L$ 档的最近倍数上（128 的方案 $\\equiv$ 高一档的 64），所以窗口只兜近处、外层 $L$ 只到 17。\n\n三者合起来，每数每档循环步数 $\\le 34 / 2^L + 1$，整个窗口机制才既正确又飞快。\n\n### 七、L = 0 的统一处理\n\n代码里 `check(0)` 直接进循环：$k = 1$ 时每个数天然是\"1 的倍数\"，$c^1 \\equiv 0$，窗口变成\"纯 $+j$ 找悬崖\"（比如 $15 \\to 16$ 不借助任何批量就能赚到）。**一套代码覆盖两件事，不用特判。**\n\n### 八、算法流程与复杂度\n\n1. 预处理 $g$ 表到 $2^{18}$\n2. 对 $L = 0 \\ldots 17$：每个数 $c^1 \\to$ 候选（最近倍数）$\\to$ 按步长 $k$ 跳格点，遇 2 的幂或增量 $\\ge 34$ 停，取 min 累加，最后 $+ L$\n3. 输出 18 个档位的最小值\n\n复杂度：预处理 $O(2^{18})$；每组询问 $\\sum_L n(1 + 34/2^L) \\approx 50n$。\n\n## 逐行代码解析\n\n### 全局变量与 g 表预处理\n\n```cpp\nconst long long sz = (long long)1 << 18;\nint g[sz + 10];\nvector<long long> a;\n```\n\n| 行 | 代码 | 解析 |\n|----|------|------|\n| 1 | `const long long sz = (1LL) << 18;` | g 表大小 $2^{18} = 262144$。用 `long long` 防止 `1 << 18` 在 32 位环境下的潜在问题 |\n| 2 | `int g[sz + 10];` | g 表，下标 $0 \\sim 2^{18}$，加 10 的余量防越界。元素值最大 33，`int` 足够 |\n| 3 | `vector<long long> a;` | 全局数组，用 `long long` 因为 $a_i + c^1$ 可能超过 `int` 范围（$10^5 + 2^{17} \\approx 2.3 \\times 10^5$，实际不超 int，但用 long long 更安全） |\n\n### main 函数中的 g 表初始化\n\n```cpp\ng[0] = 0;\nfor (int i = 1; i <= sz; i++)\n{\n    if (i & 1) g[i] = g[i - 1] + 1;\n    else g[i] = g[i >> 1] + 1;\n}\n```\n\n| 行 | 代码 | 解析 |\n|----|------|------|\n| 1 | `g[0] = 0;` | 边界：0 的操作数为 0 |\n| 3 | `if (i & 1) g[i] = g[i - 1] + 1;` | 奇数：先减 1 变偶数，再走 $g[i-1]$ 的路径 |\n| 4 | `else g[i] = g[i >> 1] + 1;` | 偶数：直接折半，走 $g[i/2]$ 的路径 |\n\n这就是 $g(x) = \\lfloor \\log_2 x \\rfloor + \\text{popcount}(x)$ 的递推实现。\n\n### check 函数：固定 L 的总花费\n\n```cpp\nlong long check(int L, int n)\n{\n    long long k = (long long)1 << L;\n    long long c2 = L;\n    long long sum13 = 0;\n```\n\n| 行 | 代码 | 解析 |\n|----|------|------|\n| 1 | `long long k = (1LL) << L;` | $k = 2^L$，批量折半的步长 |\n| 2 | `long long c2 = L;` | $c^2 = L$，全员共享的团购轮数 |\n| 3 | `long long sum13 = 0;` | $\\sum (c^1 + c^3)$，所有元素的\"垫付 + 单打\"之和 |\n\n### check 函数：每个数的窗口搜索\n\n```cpp\n    for (int i = 1; i <= n; i++)\n    {\t\t\n        long long c1 = (a[i] % k) ? (k - (a[i] % k)) : 0;\n        long long c3 = g[(a[i] + c1) >> L];\n        long long sum = c1 + c3;\n        int cnt = 1;\n```\n\n| 行 | 代码 | 解析 |\n|----|------|------|\n| 2 | `c1 = (a[i] % k) ? (k - (a[i] % k)) : 0;` | 垫到最近 $2^L$ 倍数的花费。若 $a_i$ 已经是倍数则 $c^1 = 0$ |\n| 3 | `c3 = g[(a[i] + c1) >> L];` | 折半后的单打花费。`(a[i]+c1) >> L` $= (a_i + c^1) / 2^L$ |\n| 4 | `sum = c1 + c3;` | 本数小账的初始值（最近倍数方案） |\n| 5 | `int cnt = 1;` | 窗口搜索计数器，从第 1 格开始跳 |\n\n```cpp\n        while (true)\n        {\n            long long tc1 = c1 + ((long long)1 << L) * cnt;\n            long long tc3 = g[((a[i] + c1) >> L) + cnt];\n            sum = min(tc1 + tc3, sum);\n            if (!((a[i] + tc1) & (a[i] + tc1 - 1)) || (((long long)1 << L) * cnt >= 34)) break;\n            cnt++;\n        }\n        sum13 += sum;\n```\n\n| 行 | 代码 | 解析 |\n|----|------|------|\n| 2 | `tc1 = c1 + ((1LL) << L) * cnt;` | 跳到第 cnt 个更远的 $2^L$ 倍数，多付 $k \\times \\text{cnt}$ |\n| 3 | `tc3 = g[((a[i]+c1) >> L) + cnt];` | 跳完之后的单打。移位值恰好 $+ \\text{cnt}$（因为多垫了 $k \\times \\text{cnt}$，移位后恰好多 cnt） |\n| 4 | `sum = min(tc1 + tc3, sum);` | 取更小的账 |\n| 5 | `if (!((a[i]+tc1) & (a[i]+tc1-1)) \\|\\| (k*cnt >= 34)) break;` | 两个终止条件（见下） |\n| 6 | `cnt++;` | **必须自增**，否则死循环 |\n| 8 | `sum13 += sum;` | 累加到全局 |\n\n**第 5 行的两个 break 条件**：\n\n1. `!(t & (t-1))`：$t = a_i + tc^1$ 是 2 的幂 $\\Rightarrow$ popcount 已达最小值 1，再跳只会 $\\lfloor \\log_2 \\rfloor$ 涨，不会更优。这是**剪枝定理②**\n2. `k * cnt >= 34`：额外垫付超过 34，就算 $g$ 降到 0 也比保底差。这是**剪枝定理①**\n\n### main 函数：枚举 L 取最小值\n\n```cpp\n    while (t--)\n    {\n        int n;\n        cin >> n;\n        a.assign(n + 1, 0);\n        for (int i = 1; i <= n; i++) cin >> a[i];\n        long long ans = LLONG_MAX;\n        for (int i = 0; i <= 17; i++) ans = min(ans, check(i, n));\n        cout << ans << '\\n';\n    }\n```\n\n| 行 | 代码 | 解析 |\n|----|------|------|\n| 5 | `ans = LLONG_MAX;` | 初始化为最大值 |\n| 6 | `for (int i = 0; i <= 17; i++) ans = min(ans, check(i, n));` | 枚举 $L = 0 \\sim 17$，取最小值。$L = 0$ 是保底（不垫，各打各的） |\n\n## 复杂度分析\n\n| 维度 | 复杂度 | 说明 |\n|------|--------|------|\n| 预处理 | $O(2^{18})$ | g 表初始化，约 $2.6 \\times 10^5$ 次操作 |\n| 每组查询 | $O(18 \\times n \\times (34/2^L + 1))$ | 18 个 L 档位，每数每档窗口步数 $\\le 34/2^L + 1$ |\n| 总计 | $\\approx O(50n)$ | $\\sum_{L=0}^{17} (1 + 34/2^L) \\approx 50$，约 $50n$ 次操作 |\n| 空间 | $O(2^{18} + n)$ | g 表 $2^{18}$ 个 int + 输入数组 |\n\n**规模感受**：$n = 10^5$，约 $5 \\times 10^6$ 次操作，2 秒内轻松通过。\n\n**为什么 $L$ 只到 17**：$2^{17} = 131072 > 10^5$，所以 $L = 17$ 时 $c^1 \\le 2^{17} - 10^5 \\approx 31072$，已经远超 34 界。更高的 $L$ 只会让垫付更大，必被 $L=0$ 支配。\n\n## 常见陷阱与注意事项\n\n> 以下是实际调试过程中踩过的**全部坑**，按踩坑顺序排列。这道题花了一整天，每个坑都是血泪教训。\n\n### 坑 1：cnt 忘自增 → 死循环\n\n| 项目 | 内容 |\n|------|------|\n| 症状 | 程序卡死或 TLE |\n| 原因 | `while(true)` 循环里忘了写 `cnt++`，永远卡在第一格 |\n| 修法 | 在 `break` 条件判断之后、循环末尾明确写 `cnt++` |\n\n### 坑 2：sum13 = c1 + c3 直接覆盖 → 答案异常小\n\n| 项目 | 内容 |\n|------|------|\n| 症状 | 输出比正确答案小很多 |\n| 原因 | 写成 `sum13 = c1 + c3` 而非 `sum13 += best`，每次循环覆盖了之前累加的结果 |\n| 修法 | 用局部 `sum` 变量存当前数的最优值，最后 `sum13 += sum` 累加到全局 |\n\n### 坑 3：逐格爬到 2 的幂 → TLE\n\n| 项目 | 内容 |\n|------|------|\n| 症状 | 超时 |\n| 原因 | 如果只判 2 的幂但不加 34 界剪枝，当 $L$ 较大时步长 $k = 2^L$ 很大，窗口内格点少没问题；但 $L = 0$ 时步长 $k = 1$，从 $a_i$ 爬到最近的 2 的幂可能要爬几十甚至上百格 |\n| 修法 | 加上 `k * cnt >= 34` 的 34 界剪枝，将窗口锁定在 $\\le 34/k + 1$ 步 |\n\n### 坑 4：x >> L + cnt 优先级雷 → WA\n\n| 项目 | 内容 |\n|------|------|\n| 症状 | 答案错误 |\n| 原因 | C++ 中 `+` 的优先级**高于** `>>`，所以 `x >> L + cnt` 被解析为 `x >> (L + cnt)` 而非 `(x >> L) + cnt` |\n| 修法 | 老实加括号：`(x >> L) + cnt`。或者写成 `g[(a[i] + c1) >> L] + cnt` 时注意先移位再加 |\n\n### 坑 5：g 表开小或 int 溢出 → RE/WA\n\n| 项目 | 内容 |\n|------|------|\n| 症状 | 运行时错误或答案错误 |\n| 原因 | g 表只开到 $10^5$ 或 $2^{17}$，但查询下标可能达到 $2^{17}$ 级别（垫付后的值移位）；或者 `1 << L` 在 `L = 17` 时如果 `1` 是 `int` 可能溢出（实际上 $2^{17}$ 不溢出，但写法不规范） |\n| 修法 | 表开 $2^{18}$；位移用 `(long long)1 << L` 或 `1LL << L`；`a` 数组用 `long long` |\n\n### 坑 6：2 的幂判断写错\n\n| 项目 | 内容 |\n|------|------|\n| 症状 | 剪枝错误，漏掉最优解或 TLE |\n| 原因 | 用 `x & (x - 1) == 0` 不加括号，被解析为 `x & ((x-1) == 0)` |\n| 修法 | 加括号：`(t & (t-1)) == 0`，或取反：`!(t & (t-1))` |\n\n### 坑 7：L = 0 时 k = 1 的退化\n\n| 项目 | 内容 |\n|------|------|\n| 症状 | 如果对 $L = 0$ 特判跳过，会漏掉\"不垫但找悬崖\"的优化（如 $15 \\to 16$ 不需要批量折半也能省 2） |\n| 原因 | 误以为 $L = 0$ 无意义 |\n| 修法 | $L = 0$ 时 $k = 1$，每个数天然是\"1 的倍数\"，$c^1 = 0$，窗口变成纯找悬崖。**一套代码覆盖 $L = 0$ 和 $L > 0$，不用特判。** |\n\n### 坑 8：long long vs int 混用\n\n| 项目 | 内容 |\n|------|------|\n| 症状 | 隐式转换导致精度丢失 |\n| 原因 | `a` 数组用 `int`，但 `a[i] + c1` 可能接近 $2.3 \\times 10^5$（不超 int），但 `a[i] + tc1`（跳格后）可能更大 |\n| 修法 | `a` 数组、`c1`、`tc1`、`sum`、`sum13`、`ans` 全部用 `long long`。g 表用 `int`（值最大 33）。`sz` 用 `long long` 防止 `1 << 18` 的类型问题 |\n\n### 坑 9：break 条件的顺序\n\n| 项目 | 内容 |\n|------|------|\n| 症状 | 如果先判 `cnt++` 再判 break，会多跳一格 |\n| 原因 | 逻辑顺序混乱 |\n| 修法 | 正确顺序：先算 `tc1`、`tc3` 并取 min $\\to$ 再判 break 条件 $\\to$ 最后 `cnt++`。这样当前格的账已经入 min，break 时不丢失 |\n\n### 坑 10：{5, 5} 口算成 11（正确是 8）\n\n| 项目 | 内容 |\n|------|------|\n| 症状 | 手算验证错误，导致怀疑代码 |\n| 原因 | $g[5] = 4$，两个 5 各打各的 $= 4 + 4 = 8$。如果误以为 $g[5] = 5$ 或忘了\"不垫才是最优\"，会算成 $5 + 5 = 10$ 或 $11$ |\n| 修法 | 垫到 $(6, 6)$ 反而 $2 + g[3] + g[3] = 2 + 3 + 3 = 8$……实际上 $g[6] = g[3] + 1 = 4$，所以 $2 + 4 + 4 = 10$ 更差。$L = 0$ 的 $8$ 才是最优。**不是所有数都值得垫**——这正是枚举 $L$ 取 min 的意义 |\n\n### 坑 11：高档接管的直觉验证\n\n| 项目 | 内容 |\n|------|------|\n| 症状 | 不理解为什么窗口只兜近处就行 |\n| 原因 | 128 的最优方案（垫到 128）在 $L = 1$ 档看起来很远（步长 2，要跳 64 格），但在 $L = 7$ 档它就是最近倍数（$c^1 = 0$）。所以远处的悬崖自然被高档接管 |\n| 修法 | 不需要修法——这是正确性保证。理解\"高档接管\"定理后自然放心 |\n\n## 对比与扩展\n\n### 暴力 vs 本算法\n\n| 维度 | 暴力枚举 | 本算法 |\n|------|---------|--------|\n| 思路 | 枚举每个数垫到哪个值 | 枚举 $L$ + 窗口搜索 |\n| 时间 | $O(n \\times \\max(a_i))$ | $O(50n)$ |\n| $n = 10^5$ | $10^{10}$（超时） | $5 \\times 10^6$（秒过） |\n\n### 核心思想提炼\n\n这道题的灵魂是两件事的**独立取优**：\n\n1. **每个数花小钱跳到附近的进位悬崖**——窗口 $\\le 34$ 管这个\n2. **全体凑出 $L$ 轮共享折半**——外层枚举 $0 \\sim 17$ 管这个\n\n窗口的三个边界——34 保底、2 的幂封顶、高档接管——分别由三条定理保证砍得安全。这不是题解语言，是自己推导出来的剪枝条件。"
    },
    {
        "id": "prob-chronostasis",
        "title": "E. 时停滞（Chronostasis）— 差分排列贪心还原",
        "oj": "",
        "problemId": "",
        "difficulty": "困难",
        "category": "基础题",
        "tags": [
            "差分数组",
            "贪心",
            "字典序最小",
            "multiset",
            "排列",
            "前缀和",
            "构造"
        ],
        "timeComplexity": "O(n log n)",
        "spaceComplexity": "O(n)",
        "codePath": "题目专辑\\基础题\\时停滞Chronostasis\\代码.cpp",
        "content": "## 题目描述\n\nYousef 有一个长度为 $n$、全部由严格正整数组成的隐藏数组 $a$。有人对 $a$ 恰好执行了一次操作得到数组 $b$：\n\n- 令 $b_1 = a_1$\n- 对 $i$ 从 $2$ 到 $n$，令 $b_i = a_i - a_{i-1}$\n\n即 $b$ 就是 $a$ 的差分数组（第一项是 $a_1$ 本身）。之后，$b$ 的元素被完全打乱。\n\n现在给你打乱后的数组 $b$。你要还原出**字典序最小**的原数组 $a$。如果不存在任何一种 $b$ 的排列能还原出全为正整数的 $a$，输出 $-1$。\n\n### 输入格式\n\n- 第一行：$t$（$t \\le 10^4$）\n- 每组数据：\n  - 第一行：$n$（$1 \\le n \\le 2 \\times 10^5$）\n  - 第二行：$n$ 个整数 $b_1, \\ldots, b_n$（$-10^9 \\le b_i \\le 10^9$）\n\n保证 $\\sum n \\le 2 \\times 10^5$。\n\n### 输出格式\n\n每组数据输出还原的 $a$（$n$ 个空格分隔的正整数），或 $-1$。\n\n## 解题思路\n\n### 第一步：问题翻译——排列 + 前缀和\n\n$b$ 打乱前的本质是：$b_1 = a_1$，后面是相邻差分。反过来看，只要把 $b$ 的 $n$ 个数排成某个顺序 $p_1, p_2, \\ldots, p_n$，就唯一确定了一个还原方案：\n\n$$a_1 = p_1, \\quad a_i = a_{i-1} + p_i = \\sum_{k=1}^{i} p_k$$\n\n所以问题变成：**把多重集 $B$ 排成一个排列，使得前缀和序列 $S_1, S_2, \\ldots, S_n$ 每一项都 $\\ge 1$，且该序列字典序最小。**\n\n注意 $S_0 = 0$ 不受限制（对应\"还没放任何数\"），但从 $S_1$ 起每一步都必须是正整数。\n\n### 第二步：总和不变量——$-1$ 判定\n\n不管怎么排列，全部加起来：\n\n$$S_n = p_1 + p_2 + \\cdots + p_n = \\text{sum}(b) = a_n$$\n\n终点是定死的，与排列无关。题目要求 $a_n \\ge 1$，所以：\n\n$$\\text{sum}(b) \\le 0 \\implies \\text{输出 } -1$$\n\n例如 $b = (3, -3)$：总和 $0$，无论怎么排最后一个 $a$ 都是 $0$，不是正整数。\n\n### 第三步：$\\text{sum}(b) \\ge 1$ 时一定有解（构造证明）\n\n把所有正数放最前面，然后 $0$，最后所有负数：\n\n- **正数阶段**：第一步落点是某个正整数 $\\ge 1$，之后只升不降，全程 $\\ge 1$\n- **负数阶段**：位置从\"正数总和 $P$\"一路降到 $T = \\text{sum}(b)$。负数的任意前缀部分和 $\\ge$ 全体负数之和（多加一个负数只会更小），所以下降途中任何时刻的位置 $\\ge T \\ge 1$，永远砸不穿地板\n\n**结论**：$-1$ 当且仅当 $\\text{sum}(b) \\le 0$；否则一定有解。\n\n### 第四步：核心引理——只要还\"活着\"，后面永远救得回来\n\n**引理**：从任意当前位置 $s \\ge 1$ 出发，剩余任意多重集 $R$，都存在一种用完 $R$ 的方式使全程 $\\ge 1$ 并恰好停在 $T$。\n\n**证明**：第三节构造的翻版——剩余正数升序全放前面（位置只升不降，从 $s \\ge 1$ 出发安全），然后 $0$，然后剩余负数（位置一路降但始终 $\\ge s + \\text{sum}(R) = T \\ge 1$）。$\\blacksquare$\n\n这个引理是整个贪心的基石：**任何合法的前缀都可以任意延拓成完整合法方案**。做字典序贪心时永远不用担心\"这一步走小了会不会把后面堵死\"——不会，后面总能走完。\n\n### 第五步：贪心策略——每一步都往\"能落到的最低点\"跳\n\n维护当前位置 $s$（初始 $0$）。每一步：\n\n1. 在剩余元素中，选**最小的满足 $s + e \\ge 1$ 的 $e$**（等价于 $e \\ge 1 - s$）\n2. 落到 $s + e$，输出，然后 $s \\mathrel{+}= e$，把 $e$ 从多重集里删掉\n\n**正确性论证**（逐位定死 + 引理）：\n\n- **第一位**：$S_1 = a_1$ 必须是 $B$ 中某个 $\\ge 1$ 的数，最小化它就是取最小正元素；由引理，这个选择必定可以走完\n- **之后每一位**：在\"前缀已是当前字典序最小\"的前提下，$S$ 的下一位 $= s + e$，在所有能保持 $\\ge 1$ 的 $e$ 中取最小即得下一位的最小可能值；由引理，任何这样的选择都可延拓。归纳即得全局字典序最小\n\n**直观图像**：这是一个从 $0$ 走到 $T$ 的小人，脚下有地板 $1$——\n\n- **能往下扎就使劲扎**：每次挑最负的\"踩得下去\"的数（$s + e \\ge 1$），一步扎到尽量深\n- **快砸穿地板时垫一垫**：如果最负的剩余数会踩到 $\\le 0$（它被\"冻结\"），就拿 $0$ 和最小的正数一点点垫高 $s$，直到那个最负的数重新踩得下去，再继续扎\n- 于是负数按从小到大被消耗，正数也按升序被消耗，两类交替进行；所有数最终都会被用掉，终点恰好是 $T$\n\n### 例子走查\n\n**例 1**：$b = \\{2, -1\\}$，$\\text{sum} = 1 \\ge 1$\n\n| 步 | 当前 $s$ | 阈值 $1-s$ | 可用元素 | 选择 | 落点 |\n|---|---|---|---|---|---|\n| 1 | 0 | 1 | 2 | 2 | 2 |\n| 2 | 2 | $-1$ | $-1$ | $-1$ | 1 |\n\n答案 $a = (2, 1)$。\n\n**例 2**：$b = \\{-5, -5, 5, 5, 6\\}$，$\\text{sum} = 6 \\ge 1$\n\n| 步 | 当前 $s$ | 阈值 $1-s$ | 剩余元素 | 可用 | 选择 | 落点 |\n|---|---|---|---|---|---|---|\n| 1 | 0 | 1 | $-5,-5,5,5,6$ | $5,5,6$ | 5 | 5 |\n| 2 | 5 | $-4$ | $-5,-5,5,6$ | $5,6$ | 5 | 10 |\n| 3 | 10 | $-9$ | $-5,-5,6$ | 全部 | $-5$ | 5 |\n| 4 | 5 | $-4$ | $-5,6$ | 只有 6 | 6 | 11 |\n| 5 | 11 | $-10$ | $-5$ | $-5$ | $-5$ | 6 |\n\n答案 $a = (5, 10, 5, 11, 6)$。注意第 4 步：$-5$ 会让位置砸到 $0$，所以必须先垫 $6$ 把 $s$ 抬到 $11$，$-5$ 才解冻——这正是\"垫一垫再扎\"。\n\n## 逐行代码解析\n\n### 输入与 $-1$ 判定\n\n```cpp\nint n;\ncin >> n;\nlong long sum = 0;\nmultiset<long long> ms;\nfor (int i = 1; i <= n; i++)\n{\n    int x;\n    cin >> x;\n    sum += x;\n    ms.insert(x);\n}\nif (sum <= 0) cout << -1 << '\\n';\n```\n\n- `sum` 累计所有 $b_i$ 的和，即终点 $a_n = T$\n- `sum <= 0`：含 $0$！不是 `< 0`。$T = 0$ 时 $a_n = 0$ 非正整数\n- `multiset<long long>`：支持重复元素 + `lower_bound` 有序查找\n- 必须 `long long`：$\\text{sum}(b)$ 可达 $2 \\times 10^5 \\times 10^9 = 2 \\times 10^{14}$\n\n### 贪心主循环\n\n```cpp\nelse\n{\n    sum = 0;                              // 复用为当前位置 s\n    for (int i = 0; i < n; i++) {\n        auto it = ms.lower_bound(1 - sum); // 找最小的 e >= 1-s\n        sum += *it;                         // 落到 s+e，更新 s\n        ms.erase(it);                       // 从多重集删除\n        cout << sum << ' ';                 // 输出当前位置（即 a_i）\n    }\n    cout << '\\n';\n}\n```\n\n- `sum = 0`：复用变量为当前位置 $s$，初始 $0$\n- `ms.lower_bound(1 - sum)`：找最小的 $e \\ge 1 - s$，即\"不跌破地板 1 的最负可用元素\"\n  - 被冻结的更小元素自动被跳过（`lower_bound` 返回第一个 $\\ge$ 阈值的迭代器）\n  - 之后阈值降低时又会自然被取到，无需手动管理冻结/解冻\n- `sum += *it`：落点 $= s + e$，即新的 $a_i$\n- **不用判空**：若所有剩余 $< \\text{阈值} \\le 0$，则全为负数，其和 $< \\text{阈值}$，但剩余和 $= T - s \\ge \\text{阈值}$（因为 $T \\ge 1$ 且 $s \\le T$），矛盾——所以 `lower_bound` 必能找到目标\n\n## 复杂度分析\n\n### 时间复杂度\n\n| 操作 | 复杂度 | 说明 |\n|---|---|---|\n| 构建 multiset | $O(n \\log n)$ | $n$ 次插入 |\n| 贪心主循环 | $O(n \\log n)$ | $n$ 次 `lower_bound` + `erase` |\n| **总计** | $O(n \\log n)$ | 排序 + $n$ 次有序查找 |\n\n### 空间复杂度\n\n| 数据 | 空间 | 说明 |\n|---|---|---|\n| multiset | $O(n)$ | 存储所有 $b_i$ |\n| **总计** | $O(n)$ | 线性空间 |\n\n## 常见陷阱与注意事项\n\n1. **$-1$ 判定是 $\\text{sum}(b) \\le 0$ 而非 $< 0$**\n   - $\\text{sum}(b) = 0$ 时 $a_n = 0$，非正整数，必须输出 $-1$\n\n2. **必须用 `long long`**\n   - $b_i$ 可达 $\\pm 10^9$，$n$ 可达 $2 \\times 10^5$，$\\text{sum}(b)$ 可达 $\\pm 2 \\times 10^{14}$\n   - 前缀和同理，中间过程可能远超 `int` 范围\n\n3. **第一步必须是 $\\ge 1$ 的正元素**\n   - 第一个数 $p_1 = a_1$，落点必须 $\\ge 1$\n   - 不能把 $0$ 或负数排在最前面——`lower_bound(1 - 0) = lower_bound(1)` 自动跳过它们\n\n4. **阈值是 $1 - s$ 不是 $-s$ 或 $0$**\n   - 合法性条件 $s + e \\ge 1$，即 $e \\ge 1 - s$\n   - 不是 $e \\ge -s$（那是 $s + e \\ge 0$，差一位）\n\n5. **贪的是\"落点\"最小，不是\"增量\"最小**\n   - 例如 $s = 1$ 时剩余 $\\{-5, 2\\}$，$e = -5$ 最小但落点是 $-4$，非法，必须选 $2$\n   - `lower_bound` 正确处理了这一点：跳过不合法的 $-5$，选 $2$\n\n6. **`lower_bound` 必能找到目标，无需判空**\n   - 若找不到，说明所有剩余元素 $< 1 - s \\le 0$，全为负数且和 $< 1 - s$\n   - 但剩余和 $= T - s \\ge 1 - s$（因 $T \\ge 1$），矛盾\n\n## 对比与扩展\n\n| 方法 | 时间 | 空间 | 特点 |\n|---|---|---|---|\n| **multiset 贪心（本算法）** | $O(n \\log n)$ | $O(n)$ | 实现简洁，自动处理冻结/解冻 |\n| 排序 + 双指针 | $O(n \\log n)$ | $O(n)$ | 排序后双指针替代 multiset，常数更优 |\n| 暴力枚举排列 | $O(n!)$ | $O(n)$ | 枚举所有排列，不可行 |\n\n### 扩展\n\n- **核心引理的普适性**：\"从任何合法状态出发都能走完\"的引理是字典序贪心的常见基石——类似思路出现在很多排列构造问题中\n- **差分数组还原**：差分数组打乱后还原原数组是一类经典问题，关键在于将差分转化为排列 + 前缀和\n- **地板约束的贪心**：每步选\"不跌破地板的最低落点\"的贪心策略，适用于所有\"维持下界 + 字典序最小\"的排列构造问题"
    },
    {
        "id": "prob-p4552-incdec-sequence",
        "title": "P4552 [Poetize6] IncDec Sequence — 差分贪心计数",
        "oj": "洛谷",
        "problemId": "P4552",
        "difficulty": "中等",
        "category": "数据结构",
        "tags": [
            "差分",
            "贪心",
            "计数",
            "差分数组"
        ],
        "timeComplexity": "O(n)",
        "spaceComplexity": "O(n)",
        "codePath": "题目专辑\\数据结构\\IncDec序列\\源.cpp",
        "content": "## 题目描述\n\n给定一个长度为 $n$ 的数列 $a_1, a_2, \\cdots, a_n$，每次可以选择一个区间 $[l, r]$，使这个区间内的数都加 $1$ 或者都减 $1$。\n\n请问至少需要多少次操作才能使数列中的所有数都一样，并求出在保证最少次数的前提下，最终得到的数列有多少种。\n\n### 输入格式\n\n- 第一行一个正整数 $n$。\n- 接下来 $n$ 行，每行一个整数，第 $i+1$ 行的整数表示 $a_i$。\n\n### 输出格式\n\n- 第一行输出最少操作次数。\n- 第二行输出最终能得到多少种结果。\n\n### 数据范围\n\n对于 $100\\%$ 的数据，$n \\le 10^5$，$0 \\le a_i \\le 2^{31}$。\n\n## 样例\n\n### 样例输入\n\n```\n4\n1\n1\n2\n2\n```\n\n### 样例输出\n\n```\n1\n2\n```\n\n## 解题思路\n\n### 一、问题转化：为什么要用差分数组\n\n原题的操作是\"选区间 $[l, r]$，把里面所有数整体 $+1$ 或 $-1$\"。如果直接在原数组上思考，每次操作影响的是一整段，状态空间非常大。但目标是\"最终所有数都相等\"，即只关心\"相邻元素的相对差异是否归零\"——这正是差分数组擅长的场景。\n\n构造差分数组：\n\n$$b[i] = a[i] - a[i-1] \\quad (i = 2, \\ldots, n), \\quad b[1] = a[1]$$\n\n则原数组 $a$ 可以由 $b$ 的前缀和唯一还原。在此对应下：\n\n$$\\text{所有数相等} \\iff b[2 \\ldots n] \\text{ 全部为 } 0$$\n\n而 $b[1]$ 的值就是那个相等的最终值。\n\n一次区间操作 $[l, r]$ 加 $1$，在差分数组上等价于：$b[l] \\mathrel{+}= 1$ 且 $b[r+1] \\mathrel{-}= 1$；减 $1$ 则符号相反。也就是说，每一次区间操作，本质上是在差分数组上挑选两个位置，一个 $+1$、另一个 $-1$。\n\n约定 $b[n+1]$ 是一个虚拟的、不影响答案的位置，对应 $r = n$ 的情形。因此每次操作等价于选择一对位置 $(i, j)$（$i, j \\in [1, n+1]$），使 $b[i]$ 加 $1$、$b[j]$ 减 $1$（或反之）。\n\n### 二、操作分类：四种配对方式\n\n把每次操作按其作用的位置分类：\n\n| 操作类型 | 作用位置 | 对 $b[2 \\ldots n]$ 的影响 | 对最终值 $b[1]$ 的影响 |\n|---|---|---|---|\n| ① 内部配对 | $b[i]$ 与 $b[j]$，$2 \\le i, j \\le n$ | 一正一负同时向 $0$ 靠拢 | 不影响 |\n| ② 与首项配对 | $b[1]$ 与 $b[i]$，$2 \\le i \\le n$ | 消掉一个正/负数 | 会改变 $b[1]$ |\n| ③ 与尾部配对 | $b[i]$ 与 $b[n+1]$，$2 \\le i \\le n$ | 消掉一个正/负数 | 不影响 |\n| ④ 首尾配对 | $b[1]$ 与 $b[n+1]$ | 无效操作 | 无意义 |\n\n类型 ④ 是浪费，应当完全避免；类型 ① 是最高效的（一次操作同时让一个正数和一个负数各走一步）；类型 ② 和 ③ 都只能消掉一个数，但类型 ② 会改变最终的 $b[1]$。\n\n### 三、第一问：最少操作次数\n\n设 $b[2 \\ldots n]$ 中：\n\n- $pos$ = 所有正数之和\n- $neg$ = 所有负数的绝对值之和\n\n由于每一步操作（无论哪种类型）最多只能让\"正数总和\"减少 $1$，同时让\"负数绝对值总和\"减少 $1$（或单独让其中一边减少 $1$），因此有下界：\n\n$$\\text{最少次数} \\ge \\max(pos, neg)$$\n\n而这个下界是可以达到的：\n\n1. 先用 $\\min(pos, neg)$ 次\"类型 ①\"操作：每次挑一个正数和一个负数，同时各 $-1$。做完后，正负两堆中必有一堆被清空，另一堆还剩下 $|pos - neg|$ 的总量。\n2. 再用 $|pos - neg|$ 次\"类型 ②/③\"操作：剩下的全正（或全负）的数，逐个用 $b[1]$（或 $b[n+1]$）来配对消去。\n\n因此：\n\n$$\\text{最少操作次数} = \\min(pos, neg) + |pos - neg| = \\max(pos, neg)$$\n\n### 四、第二问：最少次数下的方案数\n\n最少总次数为 $\\max(pos, neg)$ 已经定死，但操作 ① 与操作 ②/③ 的具体分配是自由的，这正是方案数的来源。\n\n关键观察：设最后使用了 $k$ 次\"类型 ②\"操作（即和 $b[1]$ 配对的次数），那么 $0 \\le k \\le |pos - neg|$。每次\"类型 ②\"操作都会使 $b[1]$ 加 $1$ 或减 $1$（取决于消的是正数还是负数），因此最终的 $b[1]$ 相对初始 $b[1]$ 偏移了某个与 $k$ 相关的量。\n\n- 当 $k = 0$ 时，$b[1]$ 不变，得到一种结果；\n- 当 $k = 1, 2, \\ldots, |pos - neg|$ 时，$b[1]$ 取到 $|pos - neg| + 1$ 个互不相同的值。\n\n不同的 $k$ 一定对应不同的最终序列（$b[1]$ 不同即序列不同），所以方案数就是 $k$ 的取值个数：\n\n$$\\text{方案数} = |pos - neg| + 1$$\n\n### 五、样例验证\n\n以样例 $a = [1, 1, 2, 2]$ 为例：\n\n构造差分：$b[1] = 1$，$b[2] = a_2 - a_1 = 0$，$b[3] = a_3 - a_2 = 1$，$b[4] = a_4 - a_3 = 0$，即 $b = [1, 0, 1, 0]$。\n\n考察 $b[2 \\ldots n] = [0, 1, 0]$：正数和 $pos = 1$，负数绝对值和 $neg = 0$。\n\n- 第一问：$\\max(pos, neg) = 1$。\n- 第二问：$|pos - neg| + 1 = 2$。\n\n与样例输出 $1$ 和 $2$ 完全吻合。\n\n### 六、思路总结\n\n整道题的解题链条可以浓缩为四步：\n\n1. **转换视角**：区间 $\\pm 1$ $\\to$ 差分数组两单点 $\\pm 1$；\n2. **明确目标**：所有数相等 $\\iff$ $b[2 \\ldots n]$ 全 $0$，$b[1]$ 即为最终公共值；\n3. **贪心求解**：优先做\"正负配对\"消去 $\\min(pos, neg)$，剩余 $|pos - neg|$ 用 $b[1]$ 或 $b[n+1]$ 兜底，总次数 $\\max(pos, neg)$；\n4. **计数**：剩余操作中分给 $b[1]$ 的次数 $k$ 有 $|pos - neg| + 1$ 种取法，每种对应不同最终序列。\n\n核心模型：看到\"区间整体加减 $+$ 关心相邻元素关系\"的组合，第一时间应该想到差分——把区间操作降维成单点操作，往往能让一个看似复杂的问题瞬间变成简单的贪心计数题。\n\n## 逐行代码解析\n\n```cpp\n#include<bits/stdc++.h>\nusing namespace std;\nint main()\n{\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr); cout.tie(nullptr);\n    int n;\n    cin >> n;\n```\n\n快读优化，读入数组长度 $n$。\n\n```cpp\n    vector<int> A(n + 1);\n    for (int i = 1; i <= n; i++) cin >> A[i];\n```\n\n读入原数组 $A$，下标从 $1$ 开始。\n\n```cpp\n    for (int i = n; i >= 1; i--) A[i] -= A[i - 1];\n```\n\n**原地差分**：从后往前遍历，将 $A[i]$ 变为 $a_i - a_{i-1}$。从后向前是关键——如果从前向后，$A[i-1]$ 已经被修改为差分值，会导致后续计算错误。差分后 $A[1] = a_1$（不变），$A[i] = b[i]$（$i \\ge 2$）。\n\n```cpp\n    long long pos = 0, neg = 0;\n    for (int i = 2; i <= n; i++)\n    {\n        if (A[i] > 0) pos += A[i];\n        else neg -= A[i];\n    }\n```\n\n遍历 $b[2 \\ldots n]$，累加正数之和 $pos$ 与负数绝对值之和 $neg$。注意使用 `long long`，因为 $a_i \\le 2^{31}$，差分值之和可能溢出 `int`。\n\n```cpp\n    cout << max(pos, neg) << '\\n';\n    cout << abs(pos - neg) + 1;\n    return 0;\n}\n```\n\n输出两个答案：\n- 最少操作次数 $= \\max(pos, neg)$\n- 方案数 $= |pos - neg| + 1$\n\n## 复杂度分析\n\n| 维度 | 复杂度 | 说明 |\n|---|---|---|\n| 时间 | $O(n)$ | 一次遍历读入 $+$ 一次遍历差分 $+$ 一次遍历统计正负和 |\n| 空间 | $O(n)$ | 存储 $n + 1$ 个元素的数组 |\n\n差分和统计均为线性扫描，没有嵌套循环或排序，整体效率极高。\n\n## 常见陷阱与注意事项\n\n1. **差分方向**：必须从后向前执行 `A[i] -= A[i-1]`。若从前向后，`A[i-1]` 已被修改为差分值而非原值，会导致后续差分全部错误。\n\n2. **数据溢出**：$a_i$ 最大为 $2^{31}$，差分值 $b[i]$ 本身在 `int` 范围内，但 $pos$ 和 $neg$ 是多个差分值的累加，可能超过 `int` 上限。必须使用 `long long` 存储 $pos$ 和 $neg$。\n\n3. **忽略 $b[1]$**：统计 $pos$ 和 $neg$ 时，遍历范围是 $[2, n]$，不包含 $b[1]$。$b[1]$ 是最终公共值，不需要被\"归零\"。\n\n4. **方案数公式的理解**：方案数为 $|pos - neg| + 1$ 而非 $|pos - neg|$，因为 $k = 0$（不与 $b[1]$ 配对，全部用 $b[n+1]$ 兜底）也是一种合法方案。\n\n5. **虚拟位置 $b[n+1]$ 的作用**：它是一个\"垃圾桶\"，吸收剩余的正数或负数，不影响最终结果。理解这一点是推导最少次数的关键。\n\n## 对比与扩展\n\n| 对比维度 | 本题（IncDec Sequence） | 普通差分模板 |\n|---|---|---|\n| 操作类型 | 区间 $+1$ 或 $-1$ | 区间加任意值 |\n| 目标 | 所有元素相等 | 还原原数组 |\n| 核心技巧 | 差分 $+$ 贪心配对 $+$ 计数 | 差分 $+$ 前缀和还原 |\n| 难点 | 方案数推导（$|pos-neg|+1$） | 无 |\n| 复杂度 | $O(n)$ | $O(n)$ |\n\n**扩展**：本题是差分思想在\"区间统一修改 $+$ 目标统一\"场景下的经典应用。类似思路可用于：\n- 区间加减使数组变成回文\n- 区间加减使数组满足某种单调性\n- 树上差分使所有节点权值相等（点差分 / 边差分的变体）"
    },
    {
        "id": "prob-p7915-palindrome",
        "title": "[CSP-S 2021] 回文（P7915）— 双栈分段贪心迭代",
        "oj": "洛谷",
        "problemId": "P7915",
        "difficulty": "困难",
        "category": "基础题",
        "tags": [
            "贪心",
            "回文构造",
            "双栈",
            "模拟",
            "CSP-S",
            "构造"
        ],
        "timeComplexity": "O(n)",
        "spaceComplexity": "O(n)",
        "codePath": "题目专辑\\基础题\\双栈分段贪心迭代算法P7915回文\\Untitled1.cpp",
        "content": "## 题目描述\n\n给定正整数 $n$ 和整数序列 $a_1, a_2, \\ldots, a_{2n}$，在这 $2n$ 个数中，$1, 2, \\ldots, n$ 分别各出现恰好 2 次。现在进行 $2n$ 次操作，目标是创建一个长度同样为 $2n$ 的序列 $b_1, b_2, \\ldots, b_{2n}$，初始时 $b$ 为空序列，每次可以进行以下两种操作之一：\n\n1. 将序列 $a$ 的**开头**元素加到 $b$ 的末尾，并从 $a$ 中移除（操作 `L`）\n2. 将序列 $a$ 的**末尾**元素加到 $b$ 的末尾，并从 $a$ 中移除（操作 `R`）\n\n目的是让 $b$ 成为一个**回文数列**，即满足对所有 $1 \\le i \\le n$，有 $b_i = b_{2n+1-i}$。判断是否能达成，如果能则输出**字典序最小**的操作方案。\n\n### 输入格式\n\n每个测试点包含多组测试数据。\n\n- 第一行：整数 $T$，表示测试数据组数\n- 对于每组数据：\n  - 第一行：正整数 $n$\n  - 第二行：$2n$ 个空格隔开的整数 $a_1, a_2, \\ldots, a_{2n}$\n\n### 输出格式\n\n对每组测试数据输出一行：\n- 若无法生成回文数列，输出 `-1`\n- 否则输出一个长度为 $2n$ 的、由 `L` 或 `R` 构成的字符串（字典序最小）\n\n### 数据规模\n\n- $1 \\le T \\le 100$\n- $1 \\le n, \\sum n \\le 5 \\times 10^5$\n- 序列中 $1, 2, \\ldots, n$ 各出现恰好 2 次\n\n### 样例\n\n**输入**：\n```\n2\n5\n4 1 2 4 5 3 1 2 3 5\n3\n1 2 3 1 2 3\n```\n\n**输出**：\n```\nLRRLLRRRRL\n-1\n```\n\n**样例解释**：第一组数据中，生成的 $b = [4, 5, 3, 1, 2, 2, 1, 3, 5, 4]$，是回文数列。\n\n## 解题思路\n\n### 第一步：回文对称性分析\n\n回文串的核心性质是**对称配对**：第 1 个和第 $2n$ 个元素相同，第 2 个和第 $2n-1$ 个相同，依此类推。这意味着每确定前半部分的一个操作，就能同步确定后半部分对应的操作。\n\n```\n结果串 b:  b1  b2  b3 ... bn  bn+1 ... b2n-1  b2n\n            ↕   ↕   ↕           ↕     ↕      ↕\n           配对1 配对2 配对3     配对3  配对2  配对1\n\n每步同时确定首尾两个位置的操作\n```\n\n### 第二步：首步选择与区间划分\n\n第一步只有两种选择：取左端（`L`）或取右端（`R`）。由于要求字典序最小，优先尝试 `L` 分支，失败再尝试 `R` 分支。\n\n以首步取 `L`（取 $a[1]$）为例：\n\n1. $a[1]$ 必须与序列中另一个相同值 $a[k]$ 配对，构成回文的首尾\n2. $a[1]$ 成为 $b$ 的第 1 个元素，$a[k]$ 成为 $b$ 的第 $2n$ 个元素\n3. 配对位置 $k$ 将剩余序列划分为两段：\n   - **$c$ 段**：$[2, k-1]$（$a[1]$ 和 $a[k]$ 之间的部分，从左到右管理）\n   - **$d$ 段**：$[k+1, 2n]$（$a[k]$ 右侧的部分，从右到左管理）\n\n```\n原序列:  a1  a2  a3 ... a(k-1)  ak  a(k+1) ... a(2n)\n         ↑    ←——— c 段 ———→  ↑    ←——— d 段 ———→\n        首步取走            配对取走\n\nc 段: ctop → ... ← cback     (正向管理)\nd 段: dback → ... ← dtop     (反向管理，dtop 从 2n 向左)\n```\n\n### 第三步：贪心匹配迭代\n\n此后每步在前端（$c$ 段左端 `ctop` 或 $d$ 段左端 `dtop`）和后端（$c$ 段右端 `cback` 或 $d$ 段右端 `dback`）中寻找相等的元素对。共有四种匹配条件：\n\n| 前端取自 | 后端取自 | 前端操作 | 后端操作 | 条件 |\n|---|---|---|---|---|\n| $c$ 段左端 | $c$ 段右端 | `L` | `L` | $a[\\text{ctop}] = a[\\text{cback}]$ 且 $\\text{ctop} \\ne \\text{cback}$ |\n| $c$ 段左端 | $d$ 段右端 | `L` | `R` | $a[\\text{ctop}] = a[\\text{dback}]$ 且 $d$ 段非空 |\n| $d$ 段左端 | $c$ 段右端 | `R` | `L` | $a[\\text{dtop}] = a[\\text{cback}]$ 且 $c$ 段非空 |\n| $d$ 段左端 | $d$ 段右端 | `R` | `R` | $a[\\text{dtop}] = a[\\text{dback}]$ 且 $\\text{dtop} \\ne \\text{dback}$ |\n\n**贪心策略**：每步优先尝试前端取 $c$ 段（操作 `L`），因为 `L` 字典序小于 `R`。若 $c$ 段前端无法与任何后端匹配，才尝试 $d$ 段前端（操作 `R`）。若两者均无法匹配，则该分支失败。\n\n### 第四步：对称性压缩\n\n利用回文对称性，每确定前端第 `idx` 步的操作，同步确定后端第 $2n - \\text{idx} + 1$ 步的操作。这样把 $2n$ 步的操作压缩为 $n$ 步迭代，每步同时填入两个位置。\n\n### 第五步：两分支覆盖\n\n- **`fn_L` 分支**：首步取 $a[1]$（左端），搜索 $a[1]$ 的配对位置 $k$，划分 $c = [2, k-1]$，$d = [k+1, 2n]$\n- **`fn_R` 分支**：首步取 $a[2n]$（右端），搜索 $a[2n]$ 的配对位置 $k$，划分 $c = [1, k-1]$，$d = [k+1, 2n-1]$\n\n由于 `L` 字典序小于 `R`，优先尝试 `fn_L`。若失败则尝试 `fn_R`。若均失败则无解，输出 $-1$。\n\n## 逐行代码解析\n\n### 全局变量与初始化\n\n```cpp\nvector<int> a;\nvector<char> s;\nint n;\n\nvoid init()\n{\n    a.clear(); s.clear();\n    a.resize(2 * n + 5);\n    s.resize(2 * n + 5);\n}\n```\n\n- `a`：输入序列，1-based 下标\n- `s`：操作方案字符串，`s[i]` 表示第 $i$ 步的操作（`L` 或 `R`）\n- `init()`：多组数据时清空并重新分配，多预留 5 个位置防越界\n\n### fn_L 分支（首步取左端）\n\n```cpp\nbool fn_L()\n{\n    int k = 2;\n    s[1] = 'L', s[2 * n] = 'L';          // 首尾操作均为 L\n    while (a[1] != a[k]) ++k;            // 找 a[1] 的配对位置 k\n    int ctop = 2, cback = k - 1;         // c 段：[2, k-1]\n    int dtop = 2 * n, dback = k + 1;     // d 段：[k+1, 2n]（反向）\n    int idx = 2;                          // 当前填充位置（从第 2 步开始）\n```\n\n- `s[1] = 'L'`：首步取左端\n- `s[2n] = 'L'`：末步取走配对元素 $a[k]$，由于 $a[k]$ 在 $a[1]$ 右侧，末步也是从左端取（此时 $a[k]$ 已成为剩余序列的左端），因此操作为 `L`\n- `while (a[1] != a[k]) ++k`：线性搜索 $a[1]$ 的配对位置（题目保证存在）\n- `dtop = 2n`：$d$ 段从右端开始，向左收缩\n\n```cpp\n    while (ctop <= cback || dtop >= dback)\n    {\n        // 条件1：c 段前端匹配 c 段后端 或 c 段前端匹配 d 段后端\n        if ((ctop <= cback) && ((a[ctop] == a[cback] && ctop != cback)\n            || (a[ctop] == a[dback] && dtop >= dback)))\n        {\n            s[idx] = 'L';                 // 前端取 c 段左端\n            if (a[ctop] == a[cback] && ctop != cback) {\n                s[2 * n - idx + 1] = 'L';  // 后端取 c 段右端\n                --cback;\n            } else {\n                s[2 * n - idx + 1] = 'R';  // 后端取 d 段右端\n                ++dback;\n            }\n            ++ctop;\n        }\n```\n\n- 条件 1 处理前端取 $c$ 段（操作 `L`）的情况\n- `ctop <= cback`：确保 $c$ 段非空\n- 子条件 A：`a[ctop] == a[cback]` 且 `ctop != cback`——$c$ 段内部首尾配对，后端也取 $c$ 段（`L`）\n- 子条件 B：`a[ctop] == a[dback]` 且 `dtop >= dback`——$c$ 段前端与 $d$ 段后端配对，后端取 $d$ 段（`R`）\n- `ctop != cback`：防止 $c$ 段只剩一个元素时自匹配\n\n```cpp\n        // 条件2：d 段前端匹配 c 段后端 或 d 段前端匹配 d 段后端\n        else if ((dtop >= dback) && ((a[dtop] == a[cback] && ctop <= cback)\n            || (a[dtop] == a[dback] && dtop != dback)))\n        {\n            s[idx] = 'R';                 // 前端取 d 段左端\n            if (a[dtop] == a[cback] && ctop <= cback) {\n                s[2 * n - idx + 1] = 'L';  // 后端取 c 段右端\n                --cback;\n            } else {\n                s[2 * n - idx + 1] = 'R';  // 后端取 d 段右端\n                ++dback;\n            }\n            --dtop;\n        }\n        else return false;                // 无匹配，分支失败\n        ++idx;\n    }\n    return true;\n}\n```\n\n- 条件 2 处理前端取 $d$ 段（操作 `R`）的情况，结构与条件 1 对称\n- 若两个条件都不满足，说明当前状态下无法继续配对，该分支失败\n\n### fn_R 分支（首步取右端）\n\n```cpp\nbool fn_R()\n{\n    int k = 1;\n    s[1] = 'R', s[2 * n] = 'L';          // 首步 R，末步 L\n    while (a[2 * n] != a[k]) ++k;        // 找 a[2n] 的配对位置 k\n    int ctop = 1, cback = k - 1;         // c 段：[1, k-1]\n    int dtop = 2 * n - 1, dback = k + 1; // d 段：[k+1, 2n-1]（反向）\n    int idx = 2;\n    // 主循环与 fn_L 完全相同\n    ...\n}\n```\n\n- `s[1] = 'R'`：首步取右端 $a[2n]$\n- `s[2n] = 'L'`：末步取走配对元素 $a[k]$，此时 $a[k]$ 已成为剩余序列的左端，操作为 `L`\n- 搜索方向变为从左找 $a[2n]$ 的配对位置\n- $d$ 段右边界变为 $2n-1$（因为 $a[2n]$ 已被取走）\n- 主循环逻辑与 `fn_L` 完全相同\n\n### 主函数\n\n```cpp\nint main()\n{\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr); cout.tie(nullptr);\n    int T;\n    cin >> T;\n    while (T--)\n    {\n        cin >> n;\n        init();\n        for (int i = 1; i <= 2 * n; i++) cin >> a[i];\n        if (fn_L()) {                    // 先尝试 L 分支（字典序更小）\n            for (int i = 1; i <= 2 * n; i++) cout << s[i];\n            cout << '\\n';\n        }\n        else if (fn_R()) {               // L 失败则尝试 R 分支\n            for (int i = 1; i <= 2 * n; i++) cout << s[i];\n            cout << '\\n';\n        }\n        else cout << -1 << '\\n';         // 两分支均失败，无解\n    }\n    return 0;\n}\n```\n\n- 优先尝试 `fn_L`（字典序更优），失败则尝试 `fn_R`\n- `fn_R` 会从头覆盖 `s` 数组，因此不需要额外重置\n- 两个分支均失败则输出 $-1$\n\n## 复杂度分析\n\n### 时间复杂度\n\n| 操作 | 复杂度 | 说明 |\n|---|---|---|\n| 配对位置搜索 | $O(n)$ | 线性扫描找 $a[1]$ 或 $a[2n]$ 的配对位置 |\n| 贪心匹配主循环 | $O(n)$ | 每步推进 `ctop` 或 `dtop`（以及 `cback`/`dback`），总共 $n$ 步 |\n| 两分支各执行一次 | $O(n)$ | 两个分支各 $O(n)$，常数倍 |\n| **总计** | $O(n)$ | 线性时间 |\n\n### 空间复杂度\n\n| 数据 | 空间 | 说明 |\n|---|---|---|\n| 序列数组 `a` | $O(n)$ | 长度 $2n+5$ |\n| 操作数组 `s` | $O(n)$ | 长度 $2n+5$ |\n| **总计** | $O(n)$ | 线性空间 |\n\n## 常见陷阱与注意事项\n\n1. **`ctop != cback` 的遗漏**\n   - 当 $c$ 段只剩一个元素时（`ctop == cback`），该元素不能同时作为前端和后端匹配自身\n   - 条件中 `ctop != cback` 防止此情况，遗漏会导致回文串中某位被重复使用\n\n2. **$d$ 段的方向**\n   - $d$ 段从右向左管理（`dtop` 从 $2n$ 递减，`dback` 从 $k+1$ 递增）\n   - 与 $c$ 段方向相反，混淆方向会导致取错元素\n\n3. **两分支的独立性**\n   - `fn_L` 失败后需完全重置状态再执行 `fn_R`\n   - `fn_R` 会从头覆盖 `s` 数组，因此不会残留 `fn_L` 的数据\n\n4. **配对位置不存在**\n   - 题目保证每个数恰好出现两次，`while (a[1] != a[k]) ++k` 必然终止\n   - 若输入不满足此条件，可能越界\n\n5. **多组数据的初始化**\n   - 每组测试数据必须调用 `init()` 清空 `a` 和 `s`，否则上一组残留数据污染当前结果\n\n6. **字典序最小的保证**\n   - 优先尝试 `fn_L`（首步 `L` 字典序小于 `R`）保证全局字典序最小\n   - 在 `fn_L` 内部，每步优先尝试前端取 $c$ 段（`L`），进一步保证局部字典序最小\n   - 这两层贪心保证了最终方案是所有合法方案中字典序最小的\n\n## 对比与扩展\n\n| 方法 | 时间 | 空间 | 特点 |\n|---|---|---|---|\n| **双栈分段贪心（本算法）** | $O(n)$ | $O(n)$ | 利用对称性压缩为 2 分支，贪心匹配 |\n| 暴力搜索 | $O(2^{2n})$ | $O(n)$ | 枚举所有操作序列，超时 |\n| 双向 BFS | $O(n \\cdot 4^n)$ | $O(n)$ | 搜索所有合法操作，仍指数级 |\n| 记忆化 DFS | $O(n^2)$ | $O(n^2)$ | 记忆化区间状态，可行但较慢 |\n\n### 扩展\n\n- **推广到元素出现次数不恰好为两次**：需更复杂的配对策略，可能需要预处理所有配对关系\n- **推广到取数后放入多个栈**：双段贪心思想可扩展到多段区间配对\n- **对称构造问题**：双段贪心思想适用于其他需要对称配对的构造问题，如括号匹配、镜像序列等"
    },
    {
        "id": "prob-particle-motion",
        "title": "粒子运动（动量守恒模拟优化）",
        "oj": "",
        "problemId": "",
        "difficulty": "中等",
        "category": "基础题",
        "tags": [
            "模拟",
            "贪心",
            "动量守恒",
            "物理类比",
            "优化"
        ],
        "timeComplexity": "O(n)",
        "spaceComplexity": "O(1)",
        "codePath": "题目专辑\\基础题\\粒子运动\\新建 文本文档.cpp",
        "content": "## 题目描述\n\n有 $n+2$ 个点排成一排，编号为 $0 \\sim n+1$。对于第 $i$ 号点有两个整数 $a_i, b_i$，其中 $0 \\le i \\le n+1$。规定初始时 $a_0 = b_0 = a_{n+1} = b_{n+1} = 0$。\n\n设你当前在第 $x$ 号点，当前的移动方向为 $y$，初始时 $x=0, y=1$（向右）。\n\n你将按如下方式移动，直到 $x, y$ 某一次变化后满足 $x=0, y=-1$（从左端离开）或 $x=n+1, y=1$（从右端离开）：\n\n- **若 $y=1$（向右）**：首先将 $x$ 增加 1，此时若 $a_x > 0$ 则将 $y$ 变成 $-1$（转向左），否则 $y$ 不变，最后再将 $a_x$ 减少 1\n- **若 $y=-1$（向左）**：首先将 $x$ 减少 1，此时若 $b_x > 0$ 则将 $y$ 变成 $1$（转向右），否则 $y$ 不变，最后再将 $b_x$ 减少 1\n\n问最后结束时 $x$ 会在第几号点（事实上了最后 $x$ 仅可能为 $0$ 或 $n+1$）。\n\n### 输入格式\n\n本题有多组测试数据。\n\n- 第一行：正整数 $T$，表示测试数据组数\n- 对于每组数据：\n  - 第一行：正整数 $n$\n  - 接下来 $n$ 行：每行两个非负整数 $a_i, b_i$（$1 \\le i \\le n$）\n\n### 输出格式\n\n对每组测试数据输出一行一个整数，表示最后结束时 $x$ 的编号（$0$ 或 $n+1$）。\n\n### 数据规模\n\n- $1 \\le T \\le 10$\n- $1 \\le n \\le 10^6$\n- $0 \\le a_i, b_i \\le 10^9$\n\n## 解题思路\n\n### 第一步：暴力模拟的问题\n\n直接按题意模拟每一步移动，时间复杂度可能高达 $O(\\sum a_i + \\sum b_i)$，在 $a_i, b_i$ 达到 $10^9$ 时显然超时。必须找到一种不逐步模拟的方法。\n\n### 第二步：物理类比——动量守恒\n\n观察移动规则的本质：\n\n- **向右移动**到达点 $i$ 时：若 $a_i > 0$ 则转向左（消耗 1 次 $a_i$），$a_i$ 减 1\n- **向左移动**到达点 $i$ 时：若 $b_i > 0$ 则转向右（消耗 1 次 $b_i$），$b_i$ 减 1\n\n这和物理学中的**动量**非常相似：\n\n- 粒子从点 0 出发，具有 **1 单位向右的初始动量**\n- 经过每个点 $i$ 时：\n  - $a_i$ 表示该点能**消耗的向右动量**（每次转向左消耗 1 单位）\n  - $b_i$ 表示该点能**提供的向右动量**（每次转向右获得 1 单位）\n- 粒子能否通过点 $i$，取决于到达该点时的动量是否足够\"冲破\"$a_i$ 的阻碍\n\n### 第三步：动量公式推导\n\n设粒子到达点 $i$ 时的动量为 $q_i$（正表示向右，负表示向左）：\n\n$$q_i = q_{i-1} - a_i + b_i$$\n\n- $q_{i-1} - a_i$：到达点 $i$ 后，先与 $a_i$ 次反弹对抗（每次消耗 1 动量并转向），剩余动量\n- $+ b_i$：点 $i$ 的 $b_i$ 次反弹会把粒子重新推向右边，补充动量\n\n**关键判断**：如果在某点 $i$，到达时的动量 $q_{i-1} \\le a_i$，说明粒子的向右动量不足以冲破 $a_i$ 的阻碍，最终会被反弹回左侧，从点 0 离开。\n\n如果粒子能通过所有 $n$ 个点（即所有点都满足 $q_{i-1} > a_i$），则从点 $n+1$ 离开。\n\n### 第四步：算法流程\n\n1. 初始动量 $q = 1$（向右）\n2. 从 $i = 1$ 到 $n$ 遍历每个点：\n   - 若 $q \\le a_i$：粒子无法通过，最终从左端离开，答案为 $0$，标记结束\n   - 否则更新动量 $q = q - a_i + b_i$\n3. 若所有点都通过，答案为 $n+1$\n\n## 逐行代码解析\n\n### 快速输入输出\n\n```cpp\ninline int read()\n{\n    int x = 0, f = 1; char ch = getchar();\n    while (ch < '0' || ch>'9') { if (ch == '-') f = -1; ch = getchar(); }\n    while (ch >= '0' && ch <= '9') { x = x * 10 + ch - 48; ch = getchar(); }\n    return x * f;\n}\ninline void write(long long x)\n{\n    static char buf[30];\n    int p = 0;\n    if (x < 0) { putchar('-'); x = -x; }\n    if (x == 0) buf[p++] = '0';\n    while (x) { buf[p++] = x % 10 + 48; x /= 10; }\n    while (p--) putchar(buf[p]);\n}\n```\n\n- `read()`：手写快速读入整数，比 `scanf`/`cin` 更快，适合 $n \\le 10^6$ 的大数据量\n- `write()`：手写快速输出 `long long`，逆序存入 `buf` 后逆序输出\n\n### 主逻辑\n\n```cpp\nint main()\n{\n    int T = read();\n    while (T--)\n    {\n        int n = read();\n        long long q = 1;       // 初始动量：1 单位向右\n        int ans = -1;           // -1 表示尚未确定答案\n        for(int i = 1; i <= n; i++)\n        {\n            long long a = read(), b = read();\n            if (ans != -1) continue;  // 已确定答案，跳过剩余输入（但仍需读入）\n            if (q <= a) ans = 0;       // 动量不足以通过点 i，从左端离开\n            q = q - a + b;             // 更新动量\n        }\n        if (ans == -1) write(n + 1);   // 所有点都通过，从右端离开\n        else write(0);\n        putchar('\\n');\n    }\n    return 0;\n}\n```\n\n- `long long q = 1`：初始动量为 1。注意必须用 `long long`，因为 $a_i, b_i$ 可达 $10^9$，累加后可能溢出 `int`\n- `if (ans != -1) continue`：一旦确定粒子从左端离开，后续点的输入仍需读入（否则输入流错乱），但不再处理逻辑\n- `if (q <= a) ans = 0`：动量不足以冲破 $a_i$，标记答案为 0（左端离开）\n- `q = q - a + b`：动量更新公式。先消耗 $a$（与 $a$ 次反弹对抗），再获得 $b$（被 $b$ 次反弹推回右边）\n\n## 复杂度分析\n\n### 时间复杂度\n\n| 操作 | 复杂度 | 说明 |\n|---|---|---|\n| 遍历 $n$ 个点 | $O(n)$ | 每个点 $O(1)$ 处理 |\n| **总计** | $O(n)$ | 线性时间 |\n\n与暴力模拟的 $O(\\sum a_i + \\sum b_i)$ 相比，从指数级降为线性。\n\n### 空间复杂度\n\n| 数据 | 空间 | 说明 |\n|---|---|---|\n| 动量 $q$ | $O(1)$ | 单变量维护 |\n| **总计** | $O(1)$ | 不需要存储数组，流式处理 |\n\n## 常见陷阱与注意事项\n\n1. **数据类型溢出**\n   - $a_i, b_i$ 可达 $10^9$，累加后动量 $q$ 可能超过 `int` 范围（$2 \\times 10^9$）\n   - 必须使用 `long long` 存储动量 $q$\n\n2. **判断条件是 $q \\le a$ 而非 $q < a$**\n   - 当 $q = a$ 时，粒子恰好消耗完所有动量，无法继续向右，最终从左端离开\n   - 只有 $q > a$ 时才能通过该点\n\n3. **确定答案后仍需读完输入**\n   - `if (ans != -1) continue` 跳过了逻辑处理，但 `read()` 仍在执行\n   - 若不读完输入，后续测试数据的输入流会错乱\n\n4. **初始动量为 1 而非 0**\n   - 粒子从点 0 出发向右移动，具有 1 单位初始动量\n   - $a_0 = b_0 = 0$，点 0 不影响动量\n\n5. **动量可能变为负数**\n   - $q = q - a + b$ 中，若 $a$ 远大于 $b$，$q$ 可能变为负数\n   - 但一旦 $q \\le a$ 就已标记答案，后续不再更新 $q$，因此不会影响结果\n\n## 对比与扩展\n\n| 方法 | 时间 | 空间 | 特点 |\n|---|---|---|---|\n| **动量守恒（本算法）** | $O(n)$ | $O(1)$ | 物理类比，流式处理 |\n| 暴力模拟 | $O(\\sum a_i + \\sum b_i)$ | $O(n)$ | 逐步模拟，超时 |\n| 前缀和预判 | $O(n)$ | $O(n)$ | 预处理前缀和数组，思路类似但空间更大 |\n\n### 扩展\n\n- **物理类比思维**：本题的核心思想是将\"反复反弹\"的过程类比为\"动量消耗与补充\"，这种将模拟问题转化为守恒量分析的方法在竞赛中非常实用\n- **类似问题**：弹球碰撞、光线反射等涉及\"方向反转\"的模拟问题，都可以尝试用守恒量（动量、能量）来简化分析\n- **流式处理**：不需要存储整个数组，逐个读入并处理，空间复杂度降为 $O(1)$"
    },
    {
        "id": "prob-two-digit-string",
        "title": "D. 两位数字符串（前缀和取模 + LCS）",
        "oj": "",
        "problemId": "",
        "difficulty": "困难",
        "category": "基础题",
        "tags": [
            "字符串",
            "前缀和",
            "取模",
            "LCS",
            "动态规划",
            "贪心转DP",
            "构造"
        ],
        "timeComplexity": "O(nm)",
        "spaceComplexity": "O(nm)（可滚动优化至 O(min(n,m))）",
        "codePath": "题目专辑\\基础题\\两位数字符串\\代码.cpp",
        "content": "## 题目描述\n\n给定两个仅由数字组成的字符串 $a$ 和 $b$。一次操作中，你可以在任意一个字符串里选择两个相邻字符，把它们替换为两者的和模 10。例如：\n\n```\n57246 → 2246  (5+7=12 → 2)\n57246 → 5946  (7+2=9)\n57246 → 5766  (2+4=6)\n57246 → 5720  (4+6=10 → 0)\n```\n\n每次操作后字符串长度恰好减 1。你可以执行任意多次操作（包括零次），必须使两个字符串相等（$|a| = |b|$ 且对应每个位置的字符都相同）。求最终得到的相同字符串的**最大可能长度**；若无法使它们相等，输出 $-1$。\n\n### 输入格式\n\n- 第一行：$t$（$1 \\le t \\le 2 \\times 10^3$）\n- 每组数据两行：字符串 $a$ 和 $b$（$1 \\le |a|, |b| \\le 5 \\times 10^3$，仅含数字）\n\n保证 $\\sum (|a| + |b|) \\le 10^4$。\n\n### 输出格式\n\n每组数据输出一个整数。\n\n## 解题思路\n\n### 第一步：操作的本质——\"切香肠\"\n\n把字符串想象成一根香肠，每次操作把相邻两段捏成一段，新段的值 = 两段之和的个位数。捏一通之后，串里的每个字符都对应原串中**连续的一段**，值 = 该段数字之和模 10。\n\n**关键结论**：任意一种\"切段方案\"都能通过捏合实现，且段与段之间互不打扰（加法可任意结合，每段内部从左往右捏到底即可）。因此问题完全等价于：\n\n> 给 $a$ 选一种切成 $L$ 段的方案，给 $b$ 也选一种切成 $L$ 段的方案，要求两者的\"每段之和模 10 的序列\"完全相同。$L$ 越大越好。\n\n### 第二步：换记账方式——前缀和模 10 的\"读数链\"\n\n逐段比较段和太繁琐，换一种记账方式：想象一个里程表从左到右扫过原串，每碰到一个切点就记录\"到目前为止扫过的所有数字之和模 10\"。\n\n设前缀和模 10 序列为 $p_0 = 0, p_1, p_2, \\ldots, p_n$（$p_0 = 0$ 为哨兵，$p_n = \\text{sum}(a) \\bmod 10$）。则：\n\n- 第 $i$ 段的值 $= (p_i - p_{i-1}) \\bmod 10$\n- 两条读数链相同 $\\iff$ 每个\"读数差\"相同 $\\iff$ 最终串逐字符相同\n\n问题转化为：找一条尽量长的读数链 $w_0 \\to w_1 \\to \\cdots \\to w_L$，使得 $a$ 和 $b$ 中都存在一串递增的切点位置，每个切点上的前缀和模 10 恰好是链上对应的值。\n\n### 第三步：链的头尾\"焊死\"\n\n- **链头**：第一个切点必然是位置 0，读数 $= 0$，没有选择\n- **链尾**：最后一个切点必然是串尾，读数 $= \\text{sum}(a) \\bmod 10 = \\text{sum}(b) \\bmod 10$（记为 $S$），也没有选择\n\n**$-1$ 的来源**：最终串的和 $\\equiv \\text{sum}(a) \\pmod{10}$，同时 $\\equiv \\text{sum}(b) \\pmod{10}$。若两者个位不同，永远不可能相等，输出 $-1$。\n\n**$-1$ 之外一定有解**：个位相同时，两边全部捏成一团，各自得到 $S$，长度 1 的公共串总可以做得到。\n\n### 第四步：核心——中间部分就是 LCS\n\n链的中间部分 $w_1, \\ldots, w_{L-1}$ 需要在 $a$ 的中间读数序列（位置 1 到 $n-1$ 的前缀和模 10）中按顺序出现，也要在 $b$ 的中间读数序列中按顺序出现——这正是**最长公共子序列（LCS）**的定义。\n\n$$\\boxed{\\text{答案} = \\text{LCS}(\\text{midA}, \\text{midB}) + 1}$$\n\n其中 $\\text{midA}$ 是 $a$ 的位置 1 到 $n-1$ 上的前缀和模 10 序列（去掉头 0 和尾 $S$），$\\text{midB}$ 同理。\n\n**为什么必须 DP、不能贪心**：例如 $\\text{midA} = [1, 2, 3]$，$\\text{midB} = [2, 3, 1]$。贪心地一见到 1 就匹配只得 1 个；而正确答案是先放过 1，匹配 2、3，得 2 个。早匹配可能堵死后面的匹配，必须用 DP 全局考虑。\n\n### 完整例子走查\n\n**例 1**：$a = 123$，$b = 321$\n\n- $a$ 前缀和模 10：$[0, 1, 3, 6]$；$b$ 前缀和模 10：$[0, 3, 5, 6]$。尾都是 6 $\\checkmark$\n- $\\text{midA} = [1, 3]$，$\\text{midB} = [3, 5]$。LCS $= [3]$，长度 1\n- 答案 $= 1 + 1 = 2$。链 $0 \\to 3 \\to 6$，读数差 $33$\n\n**例 2**：$a = 3111$，$b = 1311$\n\n- $a$ 读数：$[0, 3, 4, 5, 6]$；$b$ 读数：$[0, 1, 4, 5, 6]$。尾都是 6 $\\checkmark$\n- $\\text{midA} = [3, 4, 5]$，$\\text{midB} = [1, 4, 5]$。LCS $= [4, 5]$，长度 2\n- 答案 $= 3$。链 $0 \\to 4 \\to 5 \\to 6$，读数差 $411$\n\n**例 3**：$a = 55$，$b = 555$。个位 $0 \\ne 5$，输出 $-1$。\n\n## 逐行代码解析\n\n### 快速输入\n\n```cpp\ninline void read(vector<int>& arr)\n{\n    arr.clear();\n    arr.emplace_back(0);      // 哨兵：位置 0 的前缀和 = 0\n    char ch = getchar();\n    while (ch < '0' || ch > '9') ch = getchar();  // 跳过非数字字符（换行等）\n    while (ch >= '0' && ch <= '9')\n    {\n        arr.emplace_back(ch - 48);  // 存原始数字值\n        ch = getchar();\n    }\n}\n```\n\n- `arr[0] = 0`：哨兵，使前缀和计算统一（$p_0 = 0$）\n- 读入后 `arr` 为 $[0, d_1, d_2, \\ldots, d_n]$，长度 $n+1$\n\n### 前缀和模 10（原地转换）\n\n```cpp\nfor (int i = 1; i < A.size(); i++) A[i] = (A[i] + A[i - 1]) % 10;\n```\n\n将原始数字序列原地转换为前缀和模 10 序列。此时 `A` 为 $[0, p_1, p_2, \\ldots, p_n]$，其中 $p_i = (\\sum_{k=1}^{i} d_k) \\bmod 10$。\n\n### LCS 动态规划\n\n```cpp\nvector<vector<int>> dp(A.size(), vector<int>(B.size(), 0));\nfor (int i = 1; i < A.size(); i++)\n{\n    for (int j = 1; j < B.size(); j++)\n    {\n        if (A[i] == B[j]) dp[i][j] = dp[i - 1][j - 1] + 1;\n        else dp[i][j] = max(dp[i - 1][j], dp[i][j - 1]);\n    }\n}\n```\n\n- **状态定义**：`dp[i][j]` = `A[1..i]` 与 `B[1..j]` 的 LCS 长度\n- **转移方程**：\n  - `A[i] == B[j]`：当前读数可以配对，`dp[i][j] = dp[i-1][j-1] + 1`\n  - 否则：取两边末尾不用的较大值，`dp[i][j] = max(dp[i-1][j], dp[i][j-1])`\n- **下标从 1 开始**：`A[0] = B[0] = 0`（哨兵），LCS 从位置 1 开始匹配，等价于\"链头 0 已自动匹配\"\n\n### 输出\n\n```cpp\nif (A.back() == B.back()) write(dp[A.size() - 1][B.size() - 1]);\nelse write(-1);\n```\n\n- `A.back()` 和 `B.back()` 分别是两串的总和模 10（链尾 $S$）\n- 若两者不同，输出 $-1$\n- 若相同，输出 `dp[n][m]`，即完整前缀和序列的 LCS 长度。由于链头 0 和链尾 $S$ 必然匹配，完整 LCS $=$ 中间 LCS $+ 2$，答案 $=$ 完整 LCS $- 1$。但代码直接用完整序列做 LCS，$\\text{dp}[n][m]$ 已经包含链头和链尾的匹配，恰好等于答案\n\n## 复杂度分析\n\n### 时间复杂度\n\n| 操作 | 复杂度 | 说明 |\n|---|---|---|\n| 前缀和模 10 | $O(n + m)$ | 两个串各一次线性扫描 |\n| LCS DP | $O(nm)$ | 二维 DP 表 |\n| **总计** | $O(nm)$ | DP 主导 |\n\n单组最坏 $n = m = 5000$，DP 约 $2.5 \\times 10^7$ 步，$\\sum(|a|+|b|) \\le 10^4$ 保证不会多组同时最大。\n\n### 空间复杂度\n\n| 实现 | 空间 | 说明 |\n|---|---|---|\n| 二维 DP 表 | $O(nm)$ | 本实现，直观 |\n| 滚动数组优化 | $O(\\min(n,m))$ | 只保留两行，适合内存受限场景 |\n\n## 常见陷阱与注意事项\n\n1. **忘记 $-1$ 判定**\n   - 总和模 10 不同时永远不可能相等，必须先检查 `A.back() == B.back()`\n\n2. **端点处理**\n   - 代码使用完整前缀和序列（含头 0 和尾 $S$）做 LCS，答案直接是 `dp[n][m]`\n   - 若用去掉头尾的中间序列做 LCS，答案 $= \\text{LCS}(\\text{midA}, \\text{midB}) + 1$\n   - 两种口径不能混用\n\n3. **LCS 不能用贪心**\n   - 贪心匹配会堵死后续匹配，必须用 DP 全局考虑\n\n4. **前缀和必须取模 10**\n   - 读数链的值是 $\\bmod 10$ 意义下的，直接比前缀和（不取模）会导致错误\n\n5. **$n = 1$ 或 $m = 1$ 的边界**\n   - 中间序列为空，在同余前提下答案为 1（两边都是单字符，已相等）\n\n6. **哨兵 `A[0] = 0` 的作用**\n   - 使前缀和计算统一，同时使 LCS 的 `dp[0][j] = dp[i][0] = 0` 自动初始化\n\n## 对比与扩展\n\n| 方法 | 时间 | 空间 | 特点 |\n|---|---|---|---|\n| **前缀和模 10 + LCS DP（本算法）** | $O(nm)$ | $O(nm)$ 或滚动 $O(\\min(n,m))$ | 问题转化 + 经典 DP |\n| 暴力模拟所有操作 | 指数级 | $O(n+m)$ | 枚举所有捏合方式，不可行 |\n| 贪心匹配 | $O(n+m)$ | $O(1)$ | 似是而非，反例已说明 |\n\n### 扩展\n\n- **问题转化思维**：将\"捏合操作\"转化为\"切段方案\"，再转化为\"前缀和读数链\"，最终归约为 LCS——这是一条经典的\"操作 $\\to$ 切分 $\\to$ 前缀和 $\\to$ 子序列\"转化链\n- **模意义下的前缀和**：前缀和取模后值域缩小到 $0 \\sim 9$，但 LCS 不能利用这一点加速（仍需 DP 保证全局最优）\n- **滚动数组优化**：LCS 只依赖上一行，可用两行滚动数组将空间降至 $O(\\min(n,m))$"
    },
    {
        "id": "prob-xor-sort",
        "title": "D1. XOR 排序（简单版）— 对齐块分段判定",
        "oj": "",
        "problemId": "",
        "difficulty": "困难",
        "category": "基础题",
        "tags": [
            "位运算",
            "XOR",
            "分块",
            "排序判定",
            "贪心",
            "二进制"
        ],
        "timeComplexity": "O(n log n)",
        "spaceComplexity": "O(n)",
        "codePath": "题目专辑\\基础题\\XOR排序\\代码.cpp",
        "content": "## 题目描述\n\n对于由 $m$ 个正整数组成的数组 $b$，定义 $f(b)$ 如下：\n\n称 $b$ 可以被 $k$-排序，若能通过任意多次执行以下操作把 $b$ 变成非降序：\n\n- 选择两个下标 $i, j$（$0 \\le i < j \\le m-1$），满足 $i \\oplus j \\le k$（条件作用在**下标**上，与元素值无关）\n- 交换 $b_i$ 和 $b_j$\n\n$f(b)$ 定义为使 $b$ 可被 $k$-排序的**最小非负整数** $k$。\n\n给定长度为 $n$ 的正整数数组 $a$（下标从 0 开始）。本版 $q = 0$，即只需对初始状态求出 $f(a)$。$\\oplus$ 表示按位异或。\n\n### 输入格式\n\n- 第一行：$t$ 组数据\n- 每组：\n  - 第一行：$n$ 和 $q$（本版 $q = 0$）\n  - 第二行：$n$ 个整数 $a_0, a_1, \\ldots, a_{n-1}$\n\n保证 $\\sum n \\le 10^6$，$1 \\le a_i \\le 10^9$。\n\n### 输出格式\n\n每组输出 1 个整数（$q+1 = 1$），即 $f(a)$。\n\n## 解题思路\n\n### 第一步：答案只能是 0 或 2 的幂\n\n这是全题的钥匙。考察一次\"交换下标 $i, j$\"的操作，若 $i \\oplus j$ 不是 2 的幂，设它最高的 1 位是 $2^b$，可以用**三次 XOR 恰为 $2^b$ 的交换**把它替换掉：\n\n```\n原始交换：i ↔ j  （XOR = i⊕j，最高位 2^b）\n分解为：\n  1. 交换 i ↔ (i+2^b)   XOR = 2^b\n  2. 交换 (i+2^b) ↔ j    XOR ≤ 2^b\n  3. 交换 i ↔ (i+2^b)   XOR = 2^b  （恢复中间点）\n```\n\n整体效果仍是\"只交换了 $i$ 和 $j$ 上的元素\"，且每步 XOR $\\le 2^b \\le i \\oplus j$。\n\n因此任何 $k$-排序方案都可以改写成\"只使用 XOR 为 2 的幂的交换\"的方案。设实际用到的最大幂是 $2^b$，则 $f(b) \\le 2^b$。对最小的 $k$ 取等——**$f(b)$ 必然是 2 的某个幂，或者数组本身已有序时为 0**。\n\n### 第二步：$k = 2^x$ 时的块结构\n\n$i \\oplus j \\le 2^x < 2^{x+1}$ 意味着 $i$ 和 $j$ 在第 $x$ 位及以上完全相同，所以它们必然落在同一个**以 $2^{x+1}$ 对齐、长度为 $2^{x+1}$ 的连续块**里。反过来，块内任意两个下标的异或值都 $< 2^{x+1}$，其最高位 $\\le 2^x$。\n\n两边合起来：\n\n> 当 $k = 2^x$ 时，数组被切成若干长度为 $2^{x+1}$ 的连续对齐块（最后一块可能不满），**块内元素可以任意重排，块与块之间绝对无法交换**。\n\n### 第三步：判定条件\n\n既然块内自由、块间冻结，\"$k = 2^x$ 能排序\"当且仅当：\n\n$$\\max(\\text{块 } j) \\le \\min(\\text{块 } j+1) \\quad \\text{对所有相邻块}$$\n\n因为块内可以任意排列（排好内部），剩下的唯一要求是前一块的最大值不超过后一块的最小值。反过来，若某处 $\\max(\\text{前}) > \\min(\\text{后})$，这两个数永远无法跨块相遇，排序必然失败。\n\n### 第四步：不同 $k$ 对应的块结构\n\n| $k$ | 块大小 | 块内自由度 | 块间要求 |\n|---|---|---|---|\n| $0$ | 无（无法交换） | — | 数组本身必须已有序 |\n| $1 = 2^0$ | $2$ | 相邻两项可交换 | 每对位置之间 $\\max \\le \\min$ |\n| $2 = 2^1$ | $4$ | 4 元素块内任意排列 | 相邻 4 块之间 $\\max \\le \\min$ |\n| $4 = 2^2$ | $8$ | 8 元素块内任意排列 | 相邻 8 块之间 $\\max \\le \\min$ |\n| $2^x$ | $2^{x+1}$ | 块内任意排列 | 相邻块之间 $\\max \\le \\min$ |\n\n$k$ 越大块越大、越容易满足。从小到大枚举 $x$，第一个通过的 $x$ 就是答案。\n\n### 第五步：算法流程\n\n1. **特判**：若数组本身已非降序，直接输出 $0$\n2. **逐层检查**：从 $k = 1$（即 $2^0$）开始，块大小取 $2k$，线性扫描维护\"到当前块为止的全局最大值 $\\text{nmax}$\"，逐块检查当前块的每个元素是否 $\\ge \\text{nmax}$\n3. **第一次通过的 $k$ 就是答案**，输出 $k$\n4. 理论上 $x$ 最大到 $\\lceil \\log_2 n \\rceil - 1$ 必然通过（块大小 $\\ge n$，只有一个块，块间条件为空）\n\n## 逐行代码解析\n\n### check 函数\n\n```cpp\nbool check(int k, int n, const vector<int>& A)\n{\n    int gap = k * 2;          // 块大小 = 2k = 2^{x+1}\n    int nmax = INT_MIN;       // 前面所有块的全局最大值\n    for (int i = 0; i < n;)\n    {\n        int j = i + gap;      // 当前块的右边界（开区间）\n        int tmin = INT_MAX, tmax = INT_MIN;\n        for (int l = i; l < min(j, n); l++)  // 遍历当前块\n        {\n            tmin = min(tmin, A[l]);\n            tmax = max(tmax, A[l]);\n        }\n        if (tmin < nmax) return false;  // 当前块最小值 < 前面块最大值 → 失败\n        nmax = tmax;                      // 更新全局最大值\n        i = min(j, n);                    // 跳到下一块\n    }\n    return true;\n}\n```\n\n- `gap = k * 2`：$k = 2^x$ 时块大小为 $2^{x+1} = 2k$\n- `nmax`：维护前面所有块的最大值，不需要对每块单独求 min——\"当前块每个元素 $\\ge \\text{nmax}$\"与\"$\\max(\\text{前块}) \\le \\min(\\text{当前块})$\"完全等价\n- `tmin < nmax`：当前块存在元素比前面所有块的最大值还小，说明跨块逆序，判定失败\n\n### 主函数\n\n```cpp\nint main()\n{\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr); cout.tie(nullptr);\n    int t;\n    cin >> t;\n    while (t--)\n    {\n        int n, q;\n        cin >> n >> q;\n        vector<int> A(n);\n        for (int i = 0; i < n; i++) cin >> A[i];\n```\n\n标准读入，关闭同步流以应对大数据量。\n\n```cpp\n        if (is_sorted(A.begin(), A.end())) cout << 0 << '\\n';\n        else\n        {\n            int k = 1;\n            while (!check(k, n, A)) k <<= 1;\n            cout << k << '\\n';\n        }\n    }\n    return 0;\n}\n```\n\n- `is_sorted` 特判 $k = 0$：数组本身已有序时答案为 0，不能靠块判定得出（否则会把\"块长 2 且块间有序\"误判成 $k = 0$）\n- `k = 1; while (!check(k, n, A)) k <<= 1`：从 $k = 2^0 = 1$ 开始，逐位左移（$k = 1, 2, 4, 8, \\ldots$），第一个通过 check 的就是答案\n\n## 复杂度分析\n\n### 时间复杂度\n\n| 操作 | 复杂度 | 说明 |\n|---|---|---|\n| is_sorted 特判 | $O(n)$ | 一次线性扫描 |\n| check 函数（每层） | $O(n)$ | 每层线性扫描整个数组 |\n| 总层数 | $O(\\log n)$ | $k$ 从 1 到 $\\le 2^{\\lceil \\log_2 n \\rceil}$ |\n| **总计** | $O(n \\log n)$ | $\\log n$ 层 $\\times$ 每层 $O(n)$ |\n\n### 空间复杂度\n\n| 数据 | 空间 | 说明 |\n|---|---|---|\n| 数组 A | $O(n)$ | 长度 $n$ |\n| **总计** | $O(n)$ | 线性空间 |\n\n## 常见陷阱与注意事项\n\n1. **必须特判 $k = 0$**\n   - `is_sorted` 判断不能省略，否则会把\"块长 2 且块间有序但块内逆序\"的情况误判为 $k = 0$\n   - $k = 0$ 意味着无法做任何交换（$i \\oplus j \\le 0$ 要求 $i = j$），数组本身必须已有序\n\n2. **块大小是 $2k$ 不是 $k$**\n   - $k = 2^x$ 对应块大小 $2^{x+1} = 2k$\n   - 代码中 `gap = k * 2` 容易写成 `gap = k` 导致整层判定错误\n\n3. **下标从 0 开始**\n   - 题目使用零基索引，块的对齐方式以 $2^{x+1}$ 为周期\n   - 若误用 1-based 下标，块边界会偏移\n\n4. **答案上限**\n   - $n = 10^6$ 时答案最多是 $2^{19} \\approx 5 \\times 10^5$，`int` 足够\n   - 但 $k <<= 1$ 最终可能到 $2^{20}$，仍在 `int` 范围内\n\n5. **输入输出的速度**\n   - $\\sum n \\le 10^6$，必须关闭同步流 `ios::sync_with_stdio(false)`\n\n6. **最后一块可能不满**\n   - `min(j, n)` 处理了最后一块不足 `gap` 长度的情况\n   - 遗漏会导致数组越界\n\n## 对比与扩展\n\n| 方法 | 时间 | 空间 | 特点 |\n|---|---|---|---|\n| **逐层检查（本算法）** | $O(n \\log n)$ | $O(n)$ | 简单直观，无需数据结构 |\n| 线段树维护（困难版） | $O(n \\log n)$ 预处理 + $O(\\log^2 n)$ 每次更新 | $O(n)$ | 支持 $q$ 次单点更新 |\n| 暴力枚举 $k$ | $O(n \\cdot \\max k)$ | $O(n)$ | $k$ 可能到 $10^9$，超时 |\n\n### 扩展\n\n- **困难版 D2**：将\"每层 $O(n)$ 重扫\"换成用线段树按块维护 max/min 与块间约束，单点更新后从叶子往上重算，思路一脉相承\n- **XOR 交换的分解技巧**：任何 XOR 交换可分解为三次 $2$ 的幂的交换，这一技巧在其他 XOR 相关问题中也有应用\n- **对齐分块思想**：$i \\oplus j \\le 2^x$ 等价于\"$i, j$ 在同一个 $2^{x+1}$ 对齐块内\"，这种将位运算条件转化为分块结构的思路在位运算题中非常实用"
    }
];
