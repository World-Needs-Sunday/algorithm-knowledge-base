---
id: dp-01bag-2d
title: '二维费用01背包'
category: 动态规划
subcategory: 背包问题
tags: ["动态规划", "背包问题", "二维费用", "01背包", "DP"]
timeComplexity: 'O(N×V×M)'
spaceComplexity: 'O(V×M)'
codePath: '动态规划\背包问题模板\2维01背包\Untitled1.cpp'
---

## 算法原理

二维费用01背包是标准01背包的自然推广：每个物品在放入背包时同时消耗两种资源（例如体积和重量、时间和金钱），背包对两种资源分别有上限 $V$ 和 $M$。目标是在不超过两种资源上限的前提下，使物品总价值最大。

状态定义 $dp[i][j]$ 表示在第一种资源消耗不超过 $i$、第二种资源消耗不超过 $j$ 时能获得的最大价值。与标准01背包不同，这里有两个维度的约束，因此状态是一个二维表格。对每个物品，需要同时从 $i$ 和 $j$ 两个方向做逆序遍历，以确保每个物品只被选取一次。

状态转移的逻辑与标准01背包完全类似：若选第 $k$ 个物品，则两种资源分别消耗 $v$ 和 $m$，剩余资源为 $i - v$ 和 $j - m$，价值为 $dp[i-v][j-m] + w$；若不选，则价值为 $dp[i][j]$。两者取较大值。由于使用一维滚动数组（空间优化），必须对 $i$ 和 $j$ 均逆序遍历。

本代码中额外添加了一个传播操作 `dp[i][j] = max(dp[i-1][j], dp[i][j-1], dp[i][j])`，其目的是确保 $dp[i][j]$ 具有"不超过"语义——即任何更小资源量对应的最优解也能传播到更大资源量的位置，使得 $dp[i][j]$ 表示的是"资源不超过 $i$ 和 $j$"而非"恰好等于 $i$ 和 $j$"。

## 状态定义与转移方程

状态定义：

$$dp[i][j] = \text{第一种资源不超过 } i \text{、第二种资源不超过 } j \text{ 时的最大价值}$$

状态转移方程（对物品 $k$，消耗 $v$ 和 $m$，价值 $w$）：

$$dp[i][j] = \max\big(dp[i][j],\; dp[i-v][j-m] + w\big), \quad i \geq v,\; j \geq m$$

传播操作（确保"不超过"语义）：

$$dp[i][j] = \max\big(dp[i-1][j],\; dp[i][j-1],\; dp[i][j]\big)$$

最终答案：

$$\text{ans} = dp[V][M]$$

## 逐行代码解析

```cpp
#include<bits/stdc++.h>
using namespace std;
int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);cout.tie(nullptr);
	int N,V,M;
	cin >> N >> V >> M;
	vector<vector<long long>> dp(V + 1,vector<long long>(M + 1,0));
	for(int k = 0;k < N;k++)
	{
		int v,m,w;
		cin >> v >> m >> w;
		for(int i = V;i >= v;i--)
		{
			for(int j = M;j >= m;j--)
			{
				dp[i][j] = max(max(dp[i - 1][j] , dp[i][j - 1]) , dp[i][j]);
				dp[i][j] = max(dp[i - v][j - m] + w,dp[i][j]); 
			}
		}
	}
	cout << dp[V][M];
} 
```

- **`dp` 表**：大小为 $(V+1) \times (M+1)$，使用 `long long` 防止溢出，初始化为 $0$。
- **物品循环 `k`**：从 $0$ 到 $N-1$，逐个处理每个物品。
- **资源遍历 `i` 和 `j`**：两者均从大到小逆序遍历（$i$ 从 $V$ 到 $v$，$j$ 从 $M$ 到 $m$），保证每个物品只被选取一次——这是01背包一维滚动数组的核心要求。
- **传播操作**：`max(max(dp[i-1][j], dp[i][j-1]), dp[i][j])` 在转移前先进行，将相邻更小资源量的最优值传播过来，确保 $dp[i][j]$ 的"不超过"语义。
- **核心转移**：`dp[i-v][j-m] + w`（选第 $k$ 个物品）与当前 `dp[i][j]`（不选）取最大值。
- **输出**：`dp[V][M]` 即为两种资源上限分别为 $V$ 和 $M$ 时的最大价值。

## 复杂度分析

- **时间复杂度**：$O(N \times V \times M)$。外层物品循环 $N$ 次，内层两重循环共 $V \times M$ 次，每次转移为 $O(1)$。
- **空间复杂度**：$O(V \times M)$。使用二维滚动数组，不需要记录每个物品的历史状态。若使用三维数组则为 $O(N \times V \times M)$，但实际中滚动数组已足够。

## 适用场景

适用于物品同时消耗两种资源且各有上限的背包问题，例如：体积和重量双限制、时间和金钱双限制、CPU 和内存双限制等场景。当资源维度增加到三个或更多时可类推推广。

## 常见陷阱与注意事项

- **双重逆序遍历**：$i$ 和 $j$ 两个维度都必须逆序遍历，任何一个维度正序都会导致同一物品被重复选取。
- **`long long` 防溢出**：价值和资源乘积可能很大，务必使用 `long long` 类型存储 `dp` 值。
- **传播操作的位置**：传播 `max(dp[i-1][j], dp[i][j-1])` 必须在核心转移之前执行，确保 $dp[i][j]$ 在转移时已经吸收了更小资源量的最优解。
- **下标越界**：逆序遍历从 $V$ 到 $v$、$M$ 到 $m$，确保 $i - v \geq 0$ 和 $j - m \geq 0$。
- **初始化值**：求最大价值时初始化为 $0$；若要求"恰好装满"两种资源，则初始化为 $-\infty$。

## 对比与扩展

与标准01背包相比，仅增加了资源维度，核心转移逻辑不变。可推广到三维甚至更高维度的费用背包，但空间和时间的增长是指数级的。若物品可重复选取则改为完全背包的二维费用版本（两个维度均正序遍历）。也可结合分组、多重等变种。
