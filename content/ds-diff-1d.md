---
id: ds-diff-1d
title: '一维差分（区间加·批量更新）'
category: 数据结构
subcategory: 前缀和与差分
tags: ["数据结构", "差分", "一维", "区间修改", "前缀和逆操作", "数学"]
timeComplexity: '修改O(1)，还原O(n)'
spaceComplexity: 'O(n)'
codePath: '数据结构\前缀和与差分\一维差分\Untitled1.cpp'
---

## 算法原理

一维差分（Difference Array）是前缀和的**逆操作**：给定原数组 $a$，构造差分数组 $d$，使得 $a$ 是 $d$ 的前缀和。差分数组的核心优势是：**区间 $[l, r]$ 同时加 $w$，只需修改差分数组的两个位置**，时间 $O(1)$。

### 核心公式

差分数组定义（$d$ 是 $a$ 的差分）：

$$d[i] = a[i] - a[i-1] \quad (d[1] = a[1])$$

换句话说，$a$ 是 $d$ 的前缀和：

$$a[i] = \sum_{k=1}^{i} d[k]$$

### 区间加操作

若将原数组 $[l, r]$ 区间内所有元素加 $w$，差分数组只需做两处修改：

$$d[l] \mathrel{+}= w, \qquad d[r+1] \mathrel{-}= w$$

**为什么只有两处变化？**
- 在 $l$ 处加 $w$：从位置 $l$ 开始，后续所有前缀和都增加 $w$。
- 在 $r+1$ 处减 $w$：从位置 $r+1$ 开始，前缀和的增量被抵消。
- 最终效果：只有 $[l, r]$ 区间内的元素增加了 $w$。

### 还原原数组

所有修改完成后，对差分数组做一次前缀和，即可得到修改后的原数组：

$$a[i] = \sum_{k=1}^{i} d[k]$$

### 复杂度

| 操作 | 时间 |
|---|---|
| 构造差分数组 | $O(n)$ |
| 单次区间加 $[l, r] + w$ | $O(1)$ |
| 还原原数组（求前缀和） | $O(n)$ |

差分适合**多次区间修改 + 最后一次性查询**的场景。若需边修改边查询，应使用树状数组或线段树。

## 数据结构图示

### 差分数组的构造

以数组 $a = [0, 3, 1, 4, 2, 5]$（下标从 1 开始，$a[0] = 0$ 为哨兵）为例，演示差分数组 $d$ 的构造：

```
原始数组 a:
  下标:   0    1    2    3    4    5
  值:     0    3    1    4    2    5

差分数组 d[i] = a[i] - a[i-1]:

d[1] = a[1] - a[0] = 3 - 0 = 3
d[2] = a[2] - a[1] = 1 - 3 = -2
d[3] = a[3] - a[2] = 4 - 1 = 3
d[4] = a[4] - a[3] = 2 - 4 = -2
d[5] = a[5] - a[4] = 5 - 2 = 3

最终差分数组 d:
  下标:   0    1    2    3    4    5    6
  值:     0    3   -2    3   -2    3    0

验证: 对 d 求前缀和即还原 a
  a[1] = d[1] = 3 ✓
  a[2] = d[1]+d[2] = 3+(-2) = 1 ✓
  a[3] = 3+(-2)+3 = 4 ✓
  a[4] = 3+(-2)+3+(-2) = 2 ✓
  a[5] = 3+(-2)+3+(-2)+3 = 5 ✓
```

差分和前缀和互为逆运算：差分"拆开"数组，前缀和"拼回"数组。

### 区间加操作示意

在 $a$ 的区间 $[2, 4]$ 上加 $w = 5$，即 $a[2], a[3], a[4]$ 都 +5：

```
操作前:
  a = [_, 3, 1, 4, 2, 5]
  d = [0, 3, -2, 3, -2, 3, 0]

操作: 区间 [2,4] + 5
  d[2] += 5    → d[2] = -2 + 5 = 3
  d[5] -= 5    → d[5] =  3 - 5 = -2

操作后的 d:
  d = [0, 3, 3, 3, -2, -2, 0]
          ↑            ↑
         +5           -5

对新的 d 求前缀和，还原 a':
  a'[1] = 3                          = 3  （不变）
  a'[2] = 3 + 3 = 6                  = 1+5 ✓
  a'[3] = 3 + 3 + 3 = 9              = 4+5 ✓
  a'[4] = 3 + 3 + 3 + (-2) = 7       = 2+5 ✓
  a'[5] = 3 + 3 + 3 + (-2) + (-2) = 5 = 5  （不变）
```

**为什么只改两个位置？**

```
位置:   1    2    3    4    5    6
d 变化: 0   +5    0    0   -5    0
        │    │    │    │    │    │
前缀和:  ↓    ↓    ↓    ↓    ↓
a'[1] =  3   (不变)
a'[2] =  3+3 = 6   ← 从这里开始 +5
a'[3] =  6+3 = 9   ← 继续 +5
a'[4] =  9+(-2) = 7 ← 继续 +5
a'[5] =  7+(-2) = 5 ← 到这里 +5 被抵消，恢复原值
        │              │
        └── +5 区间 ──┘
```

$d[l] + w$ 使从 $l$ 开始的所有前缀和都 $+w$，$d[r+1] - w$ 使从 $r+1$ 开始的前缀和抵消增量。两者配合，精确控制区间 $[l, r]$。

### 多次区间加叠加

如果有多个区间加操作，差分数组会叠加变化。例如再加一次 $[1, 3] + 2$：

```
第一次操作 [2,4]+5 后: d = [0, 3, 3, 3, -2, -2, 0]
第二次操作 [1,3]+2:
  d[1] += 2  → d[1] = 3+2 = 5
  d[4] -= 2  → d[4] = -2+(-2) = -4

最终 d = [0, 5, 3, 3, -4, -2, 0]

还原 a:
  a[1] = 5              = 3+2 ✓        （+2 生效）
  a[2] = 5+3 = 8        = 1+5+2 ✓      （+5 和 +2 都生效）
  a[3] = 5+3+3 = 11     = 4+5+2 ✓      （+5 和 +2 都生效）
  a[4] = 11+(-4) = 7    = 2+5 ✓        （+5 生效，+2 抵消）
  a[5] = 7+(-2) = 5     = 5 ✓          （都抵消，恢复）
```

差分数组天然支持多次区间操作的叠加，每次只需 $O(1)$，最后统一还原。

## 逐行代码解析

```cpp
#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);
    int N, T;
    cin >> N >> T;
    vector<long long> A(N + 2);          // 开 N+2 防止 r+1 越界
    for(int i = 1; i <= N; i++) cin >> A[i];
    for(int i = N; i >= 1; i--) A[i] = A[i] - A[i - 1];   // 原地求差分
    for(int i = 1; i <= T; i++)
    {
        int l, r;
        long long w;
        cin >> l >> r >> w;
        A[l] += w;                       // 区间左端点 +w
        A[r + 1] -= w;                    // 区间右端点后一位 -w
    }
    for(int i = 1; i <= N; i++)
    {
        A[i] += A[i - 1];                 // 前缀和还原原数组
        cout << A[i] << ' ';
    }
    return 0;
}
```

逐段分析：

- **`vector<long long> A(N + 2)`**：开 $N+2$ 空间（下标 $0 \sim N+1$），因为 $r+1$ 最大为 $N+1$，需确保不越界。
- **`for(int i = N; i >= 1; i--) A[i] = A[i] - A[i - 1]`**：原地构造差分数组。**倒序遍历**是关键——若正序遍历，`A[i-1]` 已经被修改为差分值，再用它算 `A[i]` 就错了。倒序保证计算 `A[i]` 时 `A[i-1]` 还是原值。
- **`A[l] += w; A[r + 1] -= w;`**：区间加操作，$O(1)$。$T$ 次操作总时间 $O(T)$。
- **`A[i] += A[i - 1]`**：前缀和还原，$O(n)$。还原后 `A[i]` 即为修改后的原值。

### 倒序求差分的原因

假设正序求差分：
- `A[1] = A[1] - A[0]`（正确，`A[0]` 还是 0）
- `A[2] = A[2] - A[1]`（错误！`A[1]` 已经被上一步改成了差分值，不再是原值）

倒序求差分：
- `A[N] = A[N] - A[N-1]`（正确，`A[N-1]` 仍是原值）
- `A[N-1] = A[N-1] - A[N-2]`（正确，`A[N-2]` 仍是原值）
- ...
- 每一步的右操作数都还没被修改，保证正确性。

## 常见陷阱与注意事项

1. **数组大小开 N+2**
   - 差分数组需要访问 `r+1`，当 $r = N$ 时 `r+1 = N+1`，数组需开到 $N+2$。
   - 若开到 $N+1$，最后一个区间修改会越界。

2. **倒序构造差分**
   - 原地构造差分数组必须倒序遍历，否则前面的差分值会覆盖原值，导致后续计算错误。
   - 若使用额外数组存差分（如 `d[]`），则正序倒序都可以：`d[i] = a[i] - a[i-1]`。

3. **差分是前缀和的逆操作**
   - 差分 → 前缀和 = 还原。前缀和 → 差分 = 还原。两者互为逆运算。
   - 记忆口诀："加左减右"（区间加 $w$，左端点 $+w$，右端点后 $-w$）。

4. **只支持区间加，不支持区间查询**
   - 差分擅长批量区间修改，但查询单点值需要先做前缀和（$O(n)$）。
   - 若需边修改边查询（在线），应使用树状数组或线段树。

5. **数据类型**
   - 与前缀和同理，多次区间加可能导致数值很大，使用 `long long` 防溢出。

## 扩展应用

### 差分 + 前缀和的组合

差分与前缀和是一对互逆操作，常见组合使用方式：
- 多次区间加 → 用差分记录 → 最后前缀和还原 → 输出结果

### 二维差分

扩展到二维矩阵，可对子矩阵进行区间加操作。还原时需先做行前缀和再做列前缀和（或反之）。

### 树上差分

配合 LCA 使用，对树上路径 $u \to v$ 上所有节点加 $w$：

$$\text{diff}[u] \mathrel{+}= w, \quad \text{diff}[v] \mathrel{+}= w, \quad \text{diff}[\text{LCA}] \mathrel{-}= w, \quad \text{diff}[\text{fa(LCA)}] \mathrel{-}= w$$

最后 DFS 求子树和即为每个节点的最终值。
